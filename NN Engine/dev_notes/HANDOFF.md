# HANDOFF — C++ engine optimization (continue-from-here)

> **▶️ LATEST (2026-06-27, latest¹⁹ — KPvK fix + tooling COMMITTED `7351992`; detector-conditioned PLACEMENT DISCONFIRMED; GO-FORWARD = swap in high-DOF king-safety + joint-tune (pre-NNUE HCE beef-up).)** Memory [[detector-conditioned-knobs]] (disconfirmed for placement), [[king-safety-design]], [[pre-nnue-strength-roadmap]], [[material-edge-overvaluation]]; dev log `dev_notes/collapse-campaign.md` (top) + `OPTIMIZATION_LOG.md`. **COMMITTED `7351992`:** the KPvK draw fix (`ENABLE_RP_KPK_DRAW` default-on, oracle-validated, byte-id WAC 252/70,150,573 / STS 1568) + Phase-2 tooling (`_kpk_oracle.py`, `vs_sf.py`, `triage_collapses.py`, detector-proof scripts, `_chesscom_gap_fens.py`, dispatcher `vs_sf`/`triage` subs, chesscom PGN). **Carries prior gated-off scaffolding** (king-safety `KING_SAFETY_MAG=0`, is_light `KS_LIGHT_MAG=0` + light mobility) — all default-off, committed default = byte-id baseline. **WHAT WE LEARNED THIS SESSION:** the recurring real-game point-holes are PLUGGED (Gap-T/Gap-P/KPvK); the residual gap is `pieces`/placement VARIANCE (term-attribution over vs-SF collapse positions; passers=0.00, so NOT a passer hole) + one under-modeled-king-safety under-read. **Detector-conditioned PLACEMENT tested cheaply offline (curated 5037 important-midgame corpus, per-piece-type × 14 detectors, ridge, held-out) → DISCONFIRMED: conditioning OVERFITS (−4.9% beyond a per-piece flat scale); placement variance is NNUE-territory.** Only generalizable gain = per-piece-type FLAT placement recal (+14.6% static, but MSE≠play → play-gate). **▶️ GO-FORWARD (user-chosen, the pre-NNUE HCE beef-up path): replace the flat `latent_threat` with the BUILT attack-unit `king_safety_score` (HIGH DOF: KS_ATT_{N,B,R,Q}, safety-table curve, shield, storm, open-files, safe-checks, batteries, phase-taper) and JOINT-TUNE.** Rationale: the term's value is its tunable DOF (not its untuned drop-in, which was Elo-flat as an ADD); and king danger — unlike placement — IS genuinely detector-driven (the conditioning that failed for placement has a real chance here). STEPS: (1) NEUTRAL structural swap — route king danger through king_safety_score INSTEAD of latent_threat (replace, not add = no double-count), find the `KING_SAFETY_MAG` that makes it ≈0-regression on WAC/STS + a short tournament (the "if 0-regression" premise to VERIFY — swap behavior differs from the prior additive test); (2) expose all DOF + build the KS static-gap corpus (`gen_kingsafety_corpus.py`/`_kingsafety_static.py` exist); (3) JOINT-tune the KS knobs TOGETHER with the per-piece placement recal (SPSA/Texel), GATED ON A TIMED TOURNAMENT (not MSE). Real variance-killer for placement remains NNUE-as-eval (the HCE beef-up is also the NNUE bootstrap if ever pursued). NOTE: `tune_data/midgame_corpus.csv` (5037 rows, regenerable via `gen_midgame_corpus.py`) is UNCOMMITTED data.
>
> **▶️ (2026-06-27, latest¹⁸ — ✅ Phase 2 fix #1 SHIPPED: rook-pawn KPvK draw (chesscom-2200 conversion loss); KPvK retrograde oracle built.)** Memory [[external-play-gaps]] (update c), [[endgame-draw-detection]], [[fixed-depth-bench-ceiling]]; dev log `dev_notes/collapse-campaign.md` (top) + `OPTIMIZATION_LOG.md`; plan `~/.claude/plans/handoff-lossless-speed-campaign-tranquil-rose.md`. **First Phase-2 worst-case hole, mined from the real external loss `selfplay/external/chesscom_2200_white.pgn`** (NN-Engine=White vs a chess.com 2200 bot: won a pawn move 29, drew). **Diagnosis = EVAL hole, NOT horizon:** a drawn lone rook-pawn KPvK read **~+4870** (`piece_value_boost`/mate-drive on a 1-pawn lead, no draw detection); d6→d18 all +4.4..+5.0 so search can't fix it. It caused the half-point loss — at move 56 the engine, seeing the resulting KPvK as +4.7, **traded rooks (Rxf5) INTO the dead draw** (SF keeps the rook, all positions = 0.00). **Fix = `ENABLE_RP_KPK_DRAW` (SHIPPED default-on):** new lone-rook-pawn KPvK case in `is_practically_drawn` (cpp_bitboard.cpp, after the KvK check) → returns 0, mirroring the existing KBP/KN rook-pawn blocks; rule `defender_dist <= min(pawn_dist, attacker_dist)` to the promo corner. **Validated against a full KPvK RETROGRADE ORACLE (`diagnostics/_kpk_oracle.py`, 83,238 states): 0 false-draws — never zeroes a WON KPvK** (the method win: an endgame oracle makes draw-detection provably safe). Position-fix: KPvK +4870→0, move-56 Rxf5→f8h8 (keeps rooks), benoni-29 still a4b5. **No-regression: WAC 252 / 70,150,573 (byte-id) + STS 1568 (identical)** — touches only rook-pawn KPvK, never in the suites. **GATE (user): a self-play tournament is uninformative for a self-play-invisible fix → ship on verifiable position-fix + no bench regression (THIS track's model).** Baseline UNCHANGED **WAC 252 / 70,150,573 / STS 1568**. **UNCOMMITTED:** the fix (default-on in tree) + reusable tooling `diagnostics/_kpk_oracle.py` + `diagnostics/_chesscom_gap_fens.py` (commit only if asked; no commit footer). **▶️ NEXT (the loop continues):** (a) diagnose the 3 tal-BOT FENs in `_tal_gap_fens` (re-confirm shipped fixes hold + look for residual holes); (b) build the **vs-SF-opponent harness** (SF as a strength-targeted PLAYER via `UCI_LimitStrength`/`UCI_Elo`, extending `tournament.py`) to scalably mine more self-play-invisible collapses; (c) per hole: eval-or-horizon → gated fix → no-regression + (oracle where applicable) → ship SOLO.
>
> **▶️ (2026-06-27, latest¹⁷ — is_light DEAD; ▶️ COLLAPSE-MINING Phase 2 = find new worst-case eval holes via SF-opponent games.)** Memory [[external-play-gaps]], [[lighteval-standpat-is-leaf]], [[fixed-depth-bench-ceiling]]; plan `~/.claude/plans/handoff-lossless-speed-campaign-tranquil-rose.md`; dev log `dev_notes/collapse-campaign.md` (top). **is_light eval-speed track CONCLUDED DEAD:** qsearch stand-pat IS the leaf eval for quiet positions → cheapening craters positional play (STS 1501→1266/1243 at equal time); improving/null-move already use cheap_eval (improving was SPRT-null); futility-light bench-AMBIGUOUS (+10 standalone vs −107 in-sweep — timed-depth is wall-clock/thread NOISY). **Both bench proxies fail for SMALL effects → hunt BIG worst-case fixes, gate on the TIMED TOURNAMENT.** (Built gated/byte-id/UNCOMMITTED: `KS_LIGHT_MAG` + light-only cheap queen/knight mobility + light KS surrogate — superseded.) **▶️ NEXT TRACK — collapse-mining Phase 2 (the +38.7 lever continued).** SOURCE DECISION: primary = **SF-opponent games** (our engine vs strength-targeted SF `UCI_LimitStrength`/`UCI_Elo` ~2200–2700; exploits our eval holes differently than our own eval → finds self-play-invisible gaps; build a small vs-SF-player harness extending `tournament.py`, the arbiter already drives SF via UCI). Immediate no-build diagnose targets: `selfplay/external/chesscom_2200_white.pgn` (NN-Engine vs 2200 bot, won-pawn-drew-rook conversion failure) + the 3 tal-BOT in `_tal_gap_fens`. Self-play corpus = LOW yield (depth-bound). LOOP: eval-or-horizon? (`fenvs`/`ourmove` rising depth) → diagnose (`eval_breakdown --fen`) → targeted fix (gated/byte-id) → no-regression on plugged gaps (`_tal_gap_fens`) + TIMED TOURNAMENT → ship SOLO. **Baseline WAC 252 / 70,150,573 / STS 1568 (collapse bundle COMMITTED `4fe05fd`). Dispatcher subs: `ourmove`/`fenvs`/`evalmode_sweep`/`ks_*`. New diagnostics committed in 4fe05fd.**
>
> **▶️ (2026-06-27, latest¹⁶ — COLLAPSE bundle SHIPPED +38.7 Elo; "STS MISPREDICTS PLAY"; king-safety built-but-flat; next = `is_light` v2 cheap-surrogate eval.)** Memory [[external-play-gaps]], [[king-safety-design]], [[fixed-depth-bench-ceiling]], [[improving-heuristic-shelved]]; dev logs `dev_notes/collapse-campaign.md` (the live track) + `dev_notes/OPTIMIZATION_LOG.md` (top entry); plan `~/.claude/plans/handoff-lossless-speed-campaign-tranquil-rose.md`. **King-safety term BUILT** (attack-unit `king_safety_score`, gated `KING_SAFETY_MAG=0`, 5–24× cheaper than latent_threat) but **Elo-FLAT** in a 793-game tournament (−0.4±28) → not shipped as a full feature; it's the cheap SURROGATE / latent_threat-replacement candidate now. **COLLAPSE-ELIMINATION campaign (the win):** re-validated the parked external-play fixes on the post-combo1 baseline and **SHIPPED a 6-knob bundle as default** (`VERIFY_MARGIN=16000` + `ENABLE_ROOK_DBLCOUNT_FIX`+`_SYM_UP` + `ENABLE_QPREC_PHASE_GATE` + `PASSER_ENEMY_CREDIT_PCT=0`+`ENABLE_PASSER_BLOCKADE_QUALITY`) — **tournament +38.7 ±27 Elo (875g, significant)**; fixes the real tal-BOT losses (benoni-29→a4b5; french passer F33 +0.93→−0.32). **NEW BASELINE WAC 252 / 70,150,573 / STS 1568 (52.3%)**, speed maintained (~162s/432k nps); recover old byte-id 252/67,931,145 by resetting the 6 knobs. **▶️ THE KEY LESSON: fixed-depth STS MISPREDICTS PLAY** — the bundle looked DESTRUCTIVE on STS (gapt+rookdblsym −77..−139, "non-additive") yet is +38.7 in TIMED games; the "never bundle" scare was a fixed-depth search-hole artifact; nearly parked Gap-P (top contributor) on its −106 STS. **⇒ gate eval/search changes on the TIMED TOURNAMENT (or depth-at-equal-time), NOT fixed-depth STS.** **NEXT TRACK — `is_light` v2 (approved plan):** the light eval currently OMITS heavy terms (why prior is_light failed at qsearch-standpat); replace omission with cheap SURROGATES (light-only queen/knight mobility + a `KS_LIGHT_MAG` king-safety surrogate) → point qsearch stand-pat (`QSTANDPAT_EVAL_MODE=2`, runs FULL eval today) at it → **PACE-tune on depth-at-equal-time (`wac_timed_depth`/`sts_timed_depth`) NOT STS** → tournament-confirm → revive improving/2-ply history with the freed budget; Phase 4 = graduate king-safety into the full eval as a latent_threat replacement (speed win at Elo-neutral). New dispatcher subs this session: `ks_static`/`ks_sweep`/`sts_sweep`/`ks_explain`/`fenvs`/`ourmove`. **State: collapse bundle UNCOMMITTED (this commit); king-safety scaffolding gated/byte-id; separate collapse-campaign Phase 2 = mine NEW gaps vs SF-opponent (user hunts online bots).**
>
> **▶️ (2026-06-25, latest¹⁵ — EVAL-QUALITY: scalar lane exhausted → pre-NNUE research → BUILD A PROPER ATTACK-UNIT KING-SAFETY TERM (approved plan, execute next).** Memory [[king-safety-design]] (the plan + model), [[pre-nnue-strength-roadmap]] (eval-structure audit), [[pawn-majority-term]] (the disconfirmation that led here); plan `~/.claude/plans/handoff-lossless-speed-campaign-tranquil-rose.md`; dev log `dev_notes/search-ebf-campaign.md` (top eval-quality §). **Chain of findings:** pawn-majority term DISCONFIRMED (static per-term attribution: the our-vs-SF gap on real positions is `pieces`/placement/`capture_gains`/`piece_value_boost`, NOT pawn-structure; scalar damping of those washed → the 110cp is VARIANCE not a scalar-fixable bias = the recurring scatter wall). User: pre-NNUE SF was ~3300+ HCE so we're missing structure/tuning. **Research (eval audit vs SF8-11 + Texel/CPW): ~200 Elo below pre-NNUE HCE = (1) king safety structurally CRUDE — `get_latent_threat_score` (cpp_bitboard.cpp ~4817) is flat per-attacker increments, NOT the attack-units→non-linear-safety-table model; no pawn shield/storm, open-files-near-king, holes/weak-squares, safe-checks, batteries; (2) no Texel joint weight-tuning (scalar move-match washes — Zurichess hit ~2750 on Texel alone).** ▶️ **APPROVED PLAN — build a proper attack-unit king-safety term:** new gated `king_safety_score(wK,bK)` beside (then replacing) get_latent_threat_score; `units = KS_ATT_{N,B,R,Q}*popcnt(attackerSet&type) + KS_ATTACK_COUNT + KS_WEAK + KS_SAFE_CHECK + KS_STORM + KS_OPEN_FILE + KS_BATTERY − KS_SHIELD`, `danger = KS_SAFETY_TABLE[units]` (precomputed non-linear lookup = additive pressure), Black-positive, **`phased = danger * phase_taper[phase_score]`** (USER REQ: taper to ~0 by endgame, smooth, no over/under-value at queens-traded transitions), `total += KING_SAFETY_MAG*phased/100`. Attacker set = `OR(attack_bitmasks over ~24 zone squares) ∩ enemy` (reuse existing attack data). **USER REQ — SPEED:** precompute KS_SAFETY_TABLE + phase_taper at init (zero hot-path division), reuse attack_bitmasks (no fresh gen), early-out when phase_score≥KS_PHASE_ZERO, O(zone) popcounts; profile nps via EVAL_PROFILE. **BUILD ORDER:** (0) `ks_explain.py <FEN>` visualizer FIRST (board + king-zone attack-heat overlay + per-component readout + before/after numbers — teaching aid + per-component verify gate); (1) core (attackers-by-type + attack-count + shield + open-files via reusable rook open-file scan ~1939 + the table); (2) storm; (3) holes; (4) safe-checks; (5) batteries (wire back commented `handle_batteries_...` ~4375). Each gated default-off, **byte-id 252/67,931,145**, then validate via STATIC-gap king-safety corpus (`gen_kingsafety_corpus.py`/`_kingsafety_static.py`/`_attrib.py`, reuse the just-built majority-corpus tooling) — STATIC gap our_static vs SF_static, NOT search (search hides it) — per-phase + guards held (STS "King safety" movematch, passer 85/200, trades 1674/3000, STS 1503, WAC 252) → PACE/Texel-tune → SPRT. **State:** combo1 + buffer pool + make_move COMMITTED (5676722); pawn-majority bundle + corpus tooling UNCOMMITTED (gated/byte-id/untracked); baseline WAC 252/67,931,145/STS 1503; production .so clean. Texel loop = the parked meta-lever after king safety.
>
> **▶️ (2026-06-25, latest¹⁴ — combo1 SHIPPED + lossless-speed DONE (committed); pivoted to EVAL-QUALITY → pawn-majority term BUILT; next = majority corpus + PACE tune.)** Memory [[search-ebf-campaign]], [[material-edge-overvaluation]] (refined), [[pawn-majority-term]] (new); plan `~/.claude/plans/handoff-lossless-speed-campaign-tranquil-rose.md`; dev log `dev_notes/search-ebf-campaign.md` (top §2026-06-25). **SHIPPED & COMMITTED:** (a) movegen lossless −34% cyc/call (`e1bcf55`: hoist node-invariant king/blockers/checkers 16×→2×, ScoredMove ordering collapse, search PROF framework); (b) `5676722` = **combo1 default-on** (`NULLMOVE_EXTRA=2 HISTORY_LMR_SCALE=2 LMP_MAX_DEPTH=5 LMP_BASE=2`) + **per-ply move-buffer pool** (g_moveBuf/g_noisyBuf, buildMoveListFromReordered/buildNoisyMoveList return a ref into per-ply storage — kills per-node move-list alloc; safe: ply monotonic, single-threaded) + make_move copy-cut + SEE-incremental default-on + the gated Phase F/G knob surface. **NEW BASELINE: WAC 252/300, 67,931,145 nodes, STS 1503 (50.1%).** **Lossless-speed lane CLOSED** — eval micros all below ±15% NPS noise (attackingLayer-hoist PROVEN a compiler no-op via `.s`-diff → verify codegen with assembly not the profiler); engine is EVAL-BOUND. **Search lane at PEAK** (combo1+improving regresses STS→1488; combo2 benoni 0/8). ⇒ **pivoted to eval-QUALITY.** **Material-edge thesis REFINED** (controlled `eval_breakdown.py --fen` per material bucket): the over-valuation is NARROW + benign (endgame, `piece_value_boost`, winning positions); **the decision-critical gap is the OPPOSITE — up-a-pawn UNDER-read (−2.4) = NO pawn-majority/candidate-passer term** (value fires only once ACTUALLY passed) → won pawn-up endgames under-convert. **BUILT (UNCOMMITTED, gated, byte-id 252/67,931,145):** phase-aware wing-majority bonus + 5-knob PACE bundle `PAWN_MAJORITY_MAG_MG/_EG` (phase-blend base) + `PAWN_MAJORITY_ADV_K`/`OUTSIDE_K`/`BLOCKADE_K` (modulators), added post-advanced-endgame-replace at the single return point in `placement_and_piece_eval`; env-read in update_cache; verified closes the under-read, neutral on equal pawns, STS flat (1501), modulators live. **Guards locked (combo1 build): passer-match 85/200 (err 997), trades 1674/3000 (Recapt/Simpl/OfferSimpl), STS 1503, WAC 252.** **CORPUS TOOLING BUILT (untracked):** `diagnostics/gen_majority_corpus.py` + `_majority_match.py` (reuse `iter_game_fens`/`Arbiter`/`run_one`; majority detector = wing pawn count, excludes actual passers to isolate the candidate gap, buckets by phase×wing×outside) → generated `suites/majorities.csv` = **304 SF-labeled positions** (137 mid / 122 early-eg / 41 late-eg). **PAWN-MAJORITY DISCONFIRMED + REDIRECTED (the key eval-quality finding).** Tuned the 5-knob bundle on the corpus → flat-to-worse. Root cause found via the CORRECT measurement: the search-based `_majority_match` was FLAWED (d10 search compensates, hides the static gap). Built `_majority_static.py` (our_static via ev_breakdown vs corpus sf_static) + `_majority_attrib.py` (per-term): **baseline static signed gap ≈ +3cp (ZERO — eval already calibrated on real majorities) and the 110cp |gap| scatter is driven by `pieces`/placement (sep +168), `capture_gains` (+64), `piece_value_boost` (+57) — NOT pawn-structure (passed_pawn_support +4, negligible).** ⇒ pawn-majority term aimed at the wrong target → PARKED gated-off. **REDIRECT: the real directional eval offenders on these positions are `capture_gains` (SEE capture-fantasy over-read) + `piece_value_boost` (material-edge over-valuation) — both already have knobs (`SCALE_CAPTURE_GAINS`, `PV_BOOST_MAG`/`PV_BOOST_PHASE_K`).** ▶️ **NEXT: PACE-tune `SCALE_CAPTURE_GAINS` + `PV_BOOST_*` on the fast STATIC-gap proxy** (`_majority_static.py`, seconds/candidate, pausable — sweep log `diagnostics/results/majority_sweep.csv`) to pull the 110cp gap down; gate held on the guards (passer 85/200, trades 1674/3000, STS 1503, WAC 252) → SPRT. Method lesson: STATIC our-vs-SF gap + per-term attribution = the right way to pick an eval lever (search proxies hide static gaps). (structure fixes partly self-play-invisible → ship on bias-closed + no-regression) → flip tuned defaults + commit the gated bundle. The over-valuation damp (`PV_BOOST_PHASE_K`) is a separate later guard-safe candidate. **State: production .so = combo1 byte-id baseline; majority bundle gated default-off/UNCOMMITTED.**
>
> **▶️ (2026-06-25, latest¹³ — Phase H LOSSLESS-speed campaign STARTED; search now profiled → MOVEGEN is a huge unmeasured ~21%, the #1 byte-id target.)** Memory [[search-ebf-campaign]], [[engine-opt-working-method]]; dev log `dev_notes/search-ebf-campaign.md` (§2026-06-25 Phase H); plan `~/.claude/plans/ok-so-since-the-joyful-church.md`. **Lever:** make existing code faster computing the EXACT same thing (byte-id, same nodes/moves → gate = exact WAC node match 258/97,507,126); zero strength risk, stacks; the user's "take verified byte-id speedups" rule. **Extended the eval PROF framework to SEARCH (UNCOMMITTED, gated `#ifdef EVAL_PROFILE` → no-op/byte-id in production):** `PROF_MOVEGEN`/`PROF_MAKEUNMAKE`/`PROF_TT_PROBE` + `PROF_BLOCK` scopes on make_move/unmake_move/`accessSearchEvalCache`/`generateLegalMovesReordered` + reset/dump auto-bracketing a real search in `get_engine_move`. Instrumented build = `PROFILE_EVAL=1 python setupAI.py build_ext --inplace`; run `PRESET=LIGHTNING python main.py` → `[eval_profile] search` on stderr; **rebuild production after (byte-id confirmed).** **WHOLE-SEARCH PROFILE:** EVAL ~68% · **MOVEGEN ~21% (11,415 cyc/call!)** · MAKEUNMAKE ~7% · TT_PROBE ~3%; hot eval terms PAWNS 20% / LATENT_THREAT 17% / CAPTURE_GAINS 17% / ROOKS 15%. ▶️ **NEXT (Step 2): optimize MOVEGEN first** (per-node vector allocations / `iota`+sort boilerplate / per-move `Move` construction / ordering-SEE in `generateLegalMovesReordered` move_gen.h ~489; reuse buffers, sort in place) — **byte-id gate (exact node match) + nps/wac_timed_depth each change**; then LATENT_THREAT rescan-restructure, PAWNS hoist, MAKEUNMAKE copy reduction. **Side items:** `improving` side-to-play audit (possible sign-inversion when `side_to_play=true`; default-off so byte-id holds — a fix could revive the SPRT-null improving); refined `is_light` (cheap-mobility approximations at stand-pat, low priority — plain is_light FAILED +10% nodes). **Honest:** NPS→Elo is modest (eval-bound) but FREE/stacking; the band-breaker remains eval-QUALITY (material-edge calibration). **State:** SEE-incremental SHIPPED default-on (byte-id); combo1 (`LMP_BASE=2 LMP_MAX_DEPTH=5 NULLMOVE_EXTRA=2 HISTORY_LMR_SCALE=2`) = best config, leg-A SPRT +13.4±37 (not sig), leg B unrun; all Phase F/G/H knobs+instrumentation gated/byte-id/UNCOMMITTED since `577d71a`. **Production .so is clean byte-id (the instrumented build was rebuilt away).**
>
> **▶️ (2026-06-25, latest¹² — eval-speed lane CLOSED; SEE-incremental shipped; engine is definitively EVAL-BOUND → pivot to eval-QUALITY.)** Memory [[search-ebf-campaign]], [[material-edge-overvaluation]]; dev log `dev_notes/search-ebf-campaign.md` (§2026-06-25). **combo1 SPRT leg A = +13.4 ±37 (n=467, not sig); leg B never run.** **SHIPPED (UNCOMMITTED, byte-id default-on): `ENABLE_SEE_INCREMENTAL`** (incremental-x-ray see(), 258/97,507,126 both ways, free ~1.3%, identical moves — the "take verified byte-id speedups" rule). **Eval-speed lane EXHAUSTED:** the overnight eval-screen showed the A-items (cheap-queen/knight, `improving` R2M150, `pdamp2+cutH` micro-tune, `IMBALANCE_SCALE=5`) are ALL net-negative on the combo1 base — **combo1 is the bench peak (STS 1503/WAC 252)**, the combined stack REGRESSES (STS 1400) → not an SPRT candidate. **`is_light`** (built: global `g_eval_light` + 4 heavy-term guards in `placement_and_piece_eval` + cache-bypass + `eval_by_mode` dispatch + `FUTILITY_EVAL_MODE`/`QSTANDPAT_EVAL_MODE`) **TESTED → SHELVED**: qsearch-light = +10% nodes / −18 WAC (the heavy terms are load-bearing at stand-pat; the ≈0-at-quiescent-leaves bet was wrong). **CONCLUSION: cannot cheapen the eval at decision sites → definitively EVAL-BOUND. The remaining real lever is eval QUALITY (material-edge calibration: the diagnosed +390cp up-a-rook bias), not speed/EBF.** ▶️ **NEXT: (a) SHIP combo1** (`LMP_BASE=2 LMP_MAX_DEPTH=5 NULLMOVE_EXTRA=2 HISTORY_LMR_SCALE=2`; bench-best, +13 lean, no regression, Gap-T/Gap-P fixes intact) — flip defaults or pool leg B if you want the significance (poor ROI on a small +13); **(b) PIVOT the campaign to eval-QUALITY** [[material-edge-overvaluation]] (screen on material-config buckets, gate in PLAY) = the actual ceiling. All Phase F/G knobs gated/byte-id/UNCOMMITTED (since `577d71a`).
>
> **▶️ (2026-06-24, latest¹¹ — EBF campaign: LMP+null-crank SHIPPED-PENDING (+12 SPRT); Phase F A-set → `combo1` = −30% nodes + STS +77, trade intact; capchain/SEE-prune/qsearch all shelved. SPRT held for user go.)** Memory [[search-ebf-campaign]] (full detail incl. tried-table), [[fixed-depth-bench-ceiling]], [[harness-concurrency-timed-uho]]; dev log `dev_notes/search-ebf-campaign.md` (§2026-06-23/24 + TRIED-TABLE + Phase F). Gap-T/Gap-P + the EBF knobs were COMMITTED `577d71a` (gated/byte-id); the Phase-F A-set knobs below are NEW/UNCOMMITTED. **EBF WIN 1 (SPRT'd):** `lmp_nm1` = `LMP_BASE=2 LMP_MAX_DEPTH=4 NULLMOVE_EXTRA=1` (aggressive LMP + new `NULLMOVE_EXTRA` knob) = −22% nodes / benoni 8/8; overnight SPRT n=1177 = **+12.4 ±23.3 Elo (positive lean, NOT sig, no regression)** → safe but not yet ship-by-Elo. **Frontier mapped:** guard-free LMP-depth+null2 ≈ −35% ceiling; **the guards we built (capchain/index-PROTECT/passer-exempt) BACKFIRE** (all-guards = +14–16% nodes). `LMR_EXTRA` leaks STS (excluded); `VERIFY_MARGIN`≠aggression (aggr+VM benoni 0/8). **Phase F (A-set, all gated/byte-id/UNCOMMITTED):** A3 qsearch knobs (`DELTA_MARGIN`/`MAX_QDEPTH` promoted to env) = DEAD END (qshare ~27% irreducible); **A1 `ENABLE_SEE_PRUNE` (post-move hang-check) = DISQUALIFIED (breaks benoni 0/8 in the aggressive combo even at margin 2000)**; **A2 `HISTORY_LMR_SCALE` (depth-scaled reduce-more) = THE FIND, STS +131 standalone**. **▶️ WINNER `combo1` = `LMP_BASE=2 LMP_MAX_DEPTH=5 NULLMOVE_EXTRA=2 HISTORY_LMR_SCALE=2`: −30% nodes (67.9M) + STS 1503 (+77) + benoni 8/8 + WAC 252.** ▶️ **NEXT: passer-corpus gate (SF-free `diagnostics/_passer_match.py` vs recorded `sf_best` — built because live-SF `move_proxy` HUNG 28min when interop keepalive dropped, no timeout) running baseline-vs-combo1; if it holds → SPRT `combo1` vs baseline (HELD for explicit user go; re-pin `nohup sleep 36000` keepalive first; `tournament 540 'LMP_BASE=2 LMP_MAX_DEPTH=5 NULLMOVE_EXTRA=2 HISTORY_LMR_SCALE=2' 4 ebf_combo1_a`, conc 4, ends <11am).** Medium-term ~3 EBF roadmap (in [[search-ebf-campaign]]): cheap-eval surrogate to revive improving/eval-futility (SEE-prune & qsearch both proved dead/dangerous here). **New uncommitted knobs:** DELTA_MARGIN/MAX_QDEPTH/HISTORY_LMR_SCALE/HISTORY_LMR_SCALE_CAP/ENABLE_SEE_PRUNE/SEE_PRUNE_MARGIN/SEE_PRUNE_MAX_DEPTH (+ committed: ENABLE_LMR_CAPCHAIN/CAPCHAIN_*/NULLMOVE_EXTRA/PROTECT_MAX_IDX). All default-off byte-id 258/97,507,126.
>
> **▶️ (2026-06-23, latest¹⁰ — external-play gaps FIXED (gated): Gap-T VERIFY_MARGIN=16000 + Gap-P passer scorer P1+C1; deep-LMR misread CORRECTED; capture-chain guard v1 too broad. SPRT the bundle overnight.)** Memory [[external-play-gaps]], [[deep-lmr-opportunity]] (corrected), [[search-ebf-campaign]]; dev log `dev_notes/external-play-gaps.md`. **(improving R2/M150 SPRT from latest⁹ RESOLVED: pooled ~1650 games = −7 ±20 Elo = NULL → improving stays shelved.)** **Gap-T (the tal-BOT 28...b5 trade) = SEARCH bug:** history-LMR over-reduces, reduced scout fails low past the flat `VERIFY_MARGIN`(6000), VERIFY skips it → axb5 buried. FIX = **`VERIFY_MARGIN` 6000→16000** (existing knob, no code): benoni-29 LIGHTNING f2g3→a4b5; WAC 258 held; STS 1426→1566(+140); sts_timed_depth +75 at equal time, depth-neutral. **Gap-P (passer danger) = EVAL** (`boost_pieces_for_supporting_passed_pawns` wrong-sign + under-weight). Knobs (all default-off/100 byte-id; corpus `diagnostics/suites/passers.csv` 405 SF-labeled via `gen_passer_corpus.py`): **P1 `PASSER_ENEMY_CREDIT_PCT`** (scale enemy-defender term only — own-support/self-block untouched), **C1 `ENABLE_PASSER_BLOCKADE_QUALITY`/`PASSER_CONTEST_PCT`** (secure-minor vs rook-contest in `getPPIncrement`), **C2 `ENABLE_PASSER_KRACE_MG`/`PASSER_KRACE_MG_PCT`/`PASSER_KRACE_MAG`** (lift `advanced_endgame_eval` king-race → `passer_realizability_delta()` into midgame + magnitude). **Clean ship tier = P1+C1: WAC 256(−2)/STS +47, F36 +0.33→−0.05 (sign fixed).** C2 = stronger (F36→−0.44) but WAC-costly = SPRT-gated. **CORRECTION: deep LMR ALREADY EXISTS** (the `get_score_for_minimizer/maximizer` helpers hold it; `alpha_beta` delegates) — so the EBF lever is forcing-line GUARDS, not "add LMR". Built `ENABLE_LMR_CAPCHAIN` (skip LMR after a capture) but **v1 TOO BROAD (17× nodes on benoni-29) → needs v2 (run-length `capture_run` / reduce-less)**. **E0 cleanup DONE: dead `VERIFY_REDUCTION_K` + passer debug prints stripped; byte-id 258/97,507,126 confirmed.** ▶️ **NEXT — sequence by time of day (don't burn the 4 workers on a tournament during the day): DAYTIME = deterministic-bench development** (wac/sts/wac_timed_depth need no SF/arbiter/worker-pool, leave the box free): (a) capture-chain guard **v2** (run-length `capture_run` / reduce-less, fix the 17× blowup); (b) **aggressive-LMR EBF** sweep (`LMR_EXTRA` + expose `DEPTH_REDUCTION`) guard-protected → `wac_timed_depth`; (c) Gap-P **C2** tuning. **NIGHT (end of day) = launch the SPRT** (the only thing that needs the whole box for hours): bundle `VERIFY_MARGIN=16000 PASSER_ENEMY_CREDIT_PCT=0 ENABLE_PASSER_BLOCKADE_QUALITY=1 PASSER_CONTEST_PCT=30` (+ any daytime-validated guard/LMR) vs baseline, LIGHTNING conc4 adjudicated, `tournament 600 '<knobs>' 4 gaps_bundle_a` → pool a `_b` continuation → ~1500-game verdict → flip defaults (ask first). All gated/byte-id/UNCOMMITTED; commits held for sign-off; ≤4 workers; interop keepalive must stay up for adjudicated tournaments ([[harness-concurrency-timed-uho]]).
>
> **▶️ (2026-06-21 pm, latest⁹ — improving R2/M150 is the SPRT candidate; old-vs-current +380 Elo milestone; interop keepalive fix.)** Memory [[improving-heuristic-shelved]], [[search-ebf-campaign]], [[harness-concurrency-timed-uho]]; dev log §Session 2026-06-21 (pm). **Exposed 2 hardcoded-formula knobs (byte-id):** `LMR_EXTRA` (extra LMR plies) + `HISTORY_BONUS_SCALE` (% on depth² history bonus). **PACE daytime screen → improving R2/M150 = the clean win:** the prior default R1/M0 was the WORST STS point (1392, −34); **R2/M150 = 1551 STS (+125)** — REDUCTION=2 + DELTA_MARGIN=150 (cheap-eval noise filter) = a strong move-quality gain (+5 timed solves, depth-flat). LMR_EXTRA=2 = depth gamble (+0.71 ply timed-depth but STS −69); HIST×2 dropped (negative interaction); imp+LMR2 bundle STS cratered (1318) → **ship improving ALONE; LMR_EXTRA = separate gamble SPRT.** ▶️ **REAL TEST (the action): SPRT improving R2/M150 vs baseline** — `ENABLE_IMPROVING=1 IMPROVING_CHEAP=1 IMPROVING_REDUCTION=2 IMPROVING_DELTA_MARGIN=150`, LIGHTNING conc4 adjudicated, tags `improv_r2m150_a` (daytime til ~10pm) + `improv_r2m150_b` (1am-11am), pooled ~1500 games for the verdict. **OLD-vs-CURRENT milestone:** rebuilt `selfplay/old CE/` at LIGHTNING (ACTIVE flip) → `tournament.py --p2-engine-dir "old CE"`: **current 89.9% / +380.6 Elo** (per-phase cur/old: depth 12.22/10.71, EBF 3.09/3.46, NPS 542/239 knps; 14/17 losses were the UHO worse-side lottery, only 3 genuinely outplayed). **⚠️ WSL interop fix (REQUIRED for adjudicated tournaments):** interop drops between short commands (idle-reboot + flaky binfmt post-outage); a keepalive (`nohup sleep 36000`) is pinning an interop-up instance — KEEP IT ALIVE; if SF breaks (`arbiter init failed`/Exec format error), `wsl.exe --shutdown` + re-pin keepalive + verify `WSLInterop=enabled` + SF runs. All non-SEE-ship changes gated/byte-id/UNCOMMITTED.
>
> **▶️ (2026-06-21, latest⁸ — SEE=300 SHIPPED; lazy-eval killed; improving-cheap parked; SEE cache/re-sort lane CLOSED.)** Memory [[search-ebf-campaign]], [[improving-heuristic-shelved]]; dev log `dev_notes/search-ebf-campaign.md` (§Session 2026-06-21). **SHIPPED & committed `5c194ae`: `SEE_EXTEND_MARGIN` default DISABLED→300** — pooled SPRT (2 runs, n=907 LIGHTNING) **+43.9 Elo [+22.8,+65.3]**. New gold **WAC 258/300, 97,507,126 nodes, STS 1426/3000 (47.5%)** (env-off `SEE_EXTEND_MARGIN=1000000` recovers old gold). **Lazy/"light" eval (skip expensive eval terms at stand-pat/futility) — KILLED by the measure-first gate:** eval cache miss 79.4% (addressable) but the light gap is ~85% `capture_gains` (median ~1.5 pawns) → can't skip the costliest term safely → low skip-rate. **improving REVIVED via `cheap_eval()` (material+PST, sign-only trend) — PARKED for SPRT:** net-+ on WAC timed-depth (+3 solves/+0.106 ply, beats full-eval improving); knobs `IMPROVING_CHEAP`/`IMPROVING_REDUCTION`/`IMPROVING_DELTA_MARGIN` exposed (byte-id). **SEE cache + qsearch SEE re-sort — CLOSED:** 199M see()/6.24-per-qnode but qsearch already well-ordered (qfmc 77.2%); cache 8.7% hit (shelved), re-sort timed-depth-neutral (shelved). **BACKLOG: incremental-SEE rewrite** (recomputes full attacker set per exchange iter; precompute non-sliders once / incremental x-ray → ~1.5–2.5×, byte-id, ~3% search ≈ +4 Elo; do the rewrite NOT the cache). Profiler re-run: PAWNS hottest eval term (20–28%), ATTACK_LAYER already cheap (cached). New byte-id diagnostics kept: `SEE_COUNT`, `qfmc`/`qcut` on `[search]`, `LIGHT_GAP_PROBE`. ▶️ **NEXT (user-decided): use the free daytime machine for PACE/SPSA prep → overnight SPRT.** Candidates: improving-cheap (+knobs) + **expose hardcoded search formulas** (`depth²` history bonus, `DEPTH_REDUCTION` table, decay) → joint-tune on reliable proxies (STS/timed-depth/nodes) → launch best as the overnight SPRT. Eval track (dynamic-conditional-eval / material-edge calibration) is the bigger ceiling, parked. All non-SEE-ship changes gated/byte-id/UNCOMMITTED.
>
> **▶️ (2026-06-21, latest⁷ — SEARCH/EBF CAMPAIGN started; LEAD = controlled extensions −27.5% nodes.)** Committed to single-thread search-efficiency (no SMP) after proving the floor is DEPTH-bound. Memory [[search-ebf-campaign]]; dev log `dev_notes/search-ebf-campaign.md`; plan rewritten to the search version. **Tune on RELIABLE proxies** (wac nodes/STS byte-deterministic + `wac_timed_depth` depth-at-equal-time — they PREDICT strength, so PACE/SPSA converges, unlike eval). Baseline WAC **261/134,429,469**, STS **52.2%** (96% first-move-cutoff = heavily optimized). **LEAD: `SEE_EXTEND_MARGIN=300` = −27.5% nodes at only −3 WAC** — the check-extension defaults to extending ALL checks (huge node adder); SEE-filtering extends only checks that sac ≤300. (SEE=0 −31.5%/−9WAC; 300 is the sweet spot.) **timed-depth decider RUNNING** (does −27.5% nodes → more depth at equal time → SPRT). **Shelved retry re-confirmed NET-NEG:** gravity+malus −1.5% STS, improving −4.6% STS, LMP inert/worse. **qsearch (Phase 2, unexplored):** wac NODES EXCLUDES `qsearchVisits`; added `qnodes=` to `[search]` line (byte-id, REBUILD owed); qsearch ordering unoptimized (`buildNoisyMoveList` filters but doesn't re-score). ▶️ **NEXT:** finish SEE timed-depth → broaden **SPSA over ALL search knobs** + **expose hardcoded formulas as knobs** (the `depth²` history bonus, `DEPTH_REDUCTION` table, decay factor — user's ask) → assemble survivors → SPRT. New diagnostic dispatcher subs from prior session still available (`runup_probe`/`cploss_probe`/etc.). All eval (S1-S4) parked gated/byte-id/UNCOMMITTED.
>
> **▶️ (2026-06-20, latest⁶ — NEW SUBPROJECT: DYNAMIC CONDITIONAL EVAL; universal layer NULL → pivot to PLACEMENT conditioning, which a detector CAN separate.)** Plan `~/.claude/plans/handoff-for-the-vectorized-meadow.md` (rewritten); memory [[dynamic-conditional-eval]]; dev log `dev_notes/dynamic-conditional-eval.md`. **Why:** scalar knobs shift a term's MEAN but the plurality of collapses (~38% level-material) is VARIANCE/scatter — only a position-conditional weight reduces variance. **BUILT + byte-id (261/134,429,469):** `mod_gain()` integer/bitwise helper (gain over 256, gated default-neutral) + 3 active modulations on the UNIVERSAL cold-tail terms — `MOD_MAT_PAWNS`/`MOD_MAT_OPPB` (material×structure), `MOD_LT_BACKING` (latent-threat×backing), `MOD_PAIR_OPEN` (pair×openness); all 4 knobs live. **RESULT — universal layer is the WRONG target for the floor:** first candidate move-flat + seeded-playout NEUTRAL (`dyn_playout` 99g Elo +3.5±80, collapse-rate 0.273 vs 0.253 = noise); `bias_sweep` confirmed only MAT_PAWNS adjusts the material buckets (and it's move-flat). The universal modulations don't fire on the level-material plurality. **KEY pivot finding (`level_sep.py`):** lost level-positions over-fire the PLACEMENT terms ~2× vs healthy (pt_bishops 1609 vs 734, pt_knights 1478 vs 695, `pieces` 1040 vs 561) — **a cheap detector DOES separate lost-level from healthy = placement magnitude.** **S4 BUILT + TESTED → NEGATIVE (the verdict):** cold-tail placement shrinkage (`MOD_PIECES_LEVEL`, byte-id, live) is WORSE monotonically (s4_playout K=128 = −16 Elo / collapse-rate 0.317 vs 0.276; s4_gentle K=48 = −10 / 0.320). ⇒ placement is CORRECT signal; high-placement level positions are genuinely SHARP and the failure is **SEARCH/HORIZON**, not eval over-credit (the magnitude detector hits 5× more healthy than lost — not selective; "does the sharp line resolve" is a search question). **▶️ FLOOR IS DEPTH-BOUND (hard data via `runup_probe`):** eval-magnitude tuning FLAT on collapse-rate (base 29/150; dampers 28-30; IMBALANCE-up flat), but **+2 plies of depth = −40% (d10 29/150 → d12 17/150)**. ⇒ the game-losing collapses are relative-depth/horizon problems; equal-depth peers mutually fail to punish what neither sees (why eval-tuning washes). **Floor lever = out-searching: EBF-lowering → SMP** (user's phase 2). `feature_sep.py`: SPACE has NO signal (don't build it); the separators (mobility/king-pressure/density) are sharpness markers = search domain. **CORRECTION (user caught my drift twice): the floor nulls do NOT show multi-variable eval < scalar — that's a SEPARATE axis (AVERAGE move quality / overall Elo), never measured.** ▶️ **OPEN NEXT (overnight): the OVERALL-strength question** — built `cploss_probe` (ACPL proxy: mean cp-loss of our move vs SF over general positions; base MEAN 33.9cp) as the sensitive inner loop; PACE-perturb the multi-variable knobs to MINIMIZE ACPL → gate winner with a GENERAL-play overall-Elo SPRT (NOT floor probes). The Tal passer (~10-ply-calculable = eval-reachable) is the class this can help. Reusable subs (prompt-free, nothing before `bash '…overnight_runner.sh'`): `runup_probe`/`cploss_probe`/`depth_probe`/`bias_sweep`/`tournament_seeded`/`flips`/`move_proxy`/`pyrun`. All S1–S4 dynamic-eval code gated/byte-id/UNCOMMITTED (engine-you-play unchanged 261/134,429,469). **PACE method validated as a PRUNER** (characterized 4 knobs + killed the universal layer in ~10 min cheap probes, no wasted playouts). **Autonomy fix:** dispatcher subs `tournament_seeded`/`flips`/`move_proxy`/`bias_sweep`/`pyrun` — prompt-free ONLY if the bash string starts with `bash '…overnight_runner.sh'` (a leading cd/export/VAR= breaks the allowlist). User roadmap: stabilize eval (variance, right terms) → EBF/depth → SMP. Memory [[dynamic-conditional-eval]], [[collapse-eval-overoptimism]].
>
> **▶️ (2026-06-18/19, latest⁵ — SCALED DIAGNOSTIC REORIENTED THE LEVER: it's MATERIAL-EDGE OVER-VALUATION, not placement.)** Built Tier 2-6 + division-gating + a realizability gate (E0) + a passer fix — **all gated/byte-id, UNCOMMITTED** (engine-you-play unchanged). Dispatcher gained `annotate`/`breakdown_tag`/`breakdown_fen`/`pattern_diag` subcommands. **Results this stretch: (1)** placement bundle (Track A) SPRT = **-0.5 ±21.5 = null** (2nd `proxy_elo` dot); scalar attack-layer boost = **-1.48%/-2.06% full-suite = REJECTED** (single-theme +44 inverted) → **scalar magnitude knobs (placement, attack) WASH; E0 imbalance-realizability DEAD (imbalance 0% of over-reads).** **(2) THE KEY FINDING** (annotate 1390 games + `pattern_diag`): the eval is **UNBIASED at equal material (-2cp scatter)** — placement is NOT the biased axis (why the bundle washed). The dominant FIXABLE **directional** bias = **MATERIAL-EDGE OVER-VALUATION** (we over-value the material-up side **+390cp up-a-rook, t=67**, escalating as we trade down: +231 mid → **+462 end**; bias-ratio 0.92-1.00). Level-but-misread positions are ENDGAMES; top error term = **`advanced_endgame_eval`** + endgame pawn placement (a dead-drawn rook endgame scored **-7.79**). **(3)** Tal loss diagnosed = MIDGAME (capture_gains material-fantasy + passer), NOT the endgame eval — passer fix (`PASSER_BLOCK_ADV`) built+correct-direction but **SPRT -10.6 ±32.1 = null, not a ship.** ▶️ **NEXT = MATERIAL CALIBRATION** (the directional lever knobs CAN fix, unlike placement scatter): instrument `piece_value_boost` (~6061) + endgame-material in `advanced_endgame_eval` + phase-dependent scales (Tier-style byte-id), **PACE-screen on MATERIAL-CONFIG BUCKETS not STS themes** (concrete objective: shrink the +390cp up-a-rook bias toward 0, hold the unbiased equal-material positions), re-probe bias via `--reeval`+`pattern_diag`, **gate in PLAY not STS**; + dig into `advanced_endgame_eval`'s gross logic mispricings (the -7.79 class — knobs won't fix that, it's correctness). Re-test reverted `ENABLE_ENDGAME_SCALE` in PLAY. **Validates the user's E3 (contextual/phase-dependent material) with data.** **USER PHILOSOPHY (carry forward):** plug the holes that cause real-game collapses even if individually ~0 Elo (small occurrence) — the two-sided gate (no large control-position degradation) is the safety; compound the plugged holes + the directional material/placement/defense knobs into ONE joint-PACE candidate that pushes boundaries; "not seeing the holes in real play is more detrimental than the near-0 aggregate Elo suggests." Memory: [[material-edge-overvaluation]], [[agentic-eval-tuning-system]]. Plan: `~/.claude/plans/handoff-for-the-vectorized-meadow.md`.
>
> **▶️ (2026-06-17, latest⁴ — GAP-TRACKING done → COMPREHENSIVE POSITIONAL KNOB COVERAGE underway.)** Gap-tracking (objective, reuse-first: flip_extract/loss_patterns/term_diag/pattern_diag/fen_vs_sf) found the #1 loss cause = **broad MIDGAME eval OVER-OPTIMISM, placement/PST-driven** (88% losses from equal, 72% collapse_from_okayish, 91% flips midgame; fen_vs_sf: 75% move-match w/ SF but eval reads +4..+8 where SF says +3..−3 = EVAL-bound). Tal-passer = a one-off, not recurring. **PACE placement tuning:** gain saturates gently — `pdamp2` (Q90/N92/B92/CTR180) = STS +1.69%, timed-depth tactics STABLE; **COMPOUND pdamp2+cut-H = +2.37% move-match** (compounds, tactics stable) → **overnight-SPRT-ready** (`SCALE_PLACE_QUEEN=90 SCALE_PLACE_KNIGHT=92 SCALE_PLACE_BISHOP=92 CENTER_INNER_MULT=180 PP_HORIZ_SUPPORT=150`), Track A awaits a machine window. **NEW DIRECTION (user): COMPREHENSIVE control-knob coverage on ALL ~25 un-knobbed positional terms** (placement+attacking+structure) so PACE jointly tunes the whole positional eval; fill only verified-net-new gaps. Audit: eval is ALREADY rich (bishop-king-zone/`CHEAP_BISHOP_KING`, `rookIncrement` open-file, pawn-attack penalties, bishop activity all exist → DON'T re-add). **PERF PATTERN (load-bearing): knobs must NOT add hot-path division — precompute scaled tables/constants at init, or gate runtime-value scalings on default.** **BUILT+committed this stretch:** placement/center magnitude knobs (`SCALE_PLACE_*`, `CENTER_INNER/OUTER_MULT`) → **refactored to precompute-at-init** (`rebuild_scaled_placement()`, zero hot-path div, byte-id 261/134,429,469); **Tier-1** scalar knobs (`IMBALANCE_SCALE`, `BISHOP_PAIR_BONUS`, `KNIGHT_PAIR_BONUS`). ▶️ NEXT = expose Tier 2-6 knobs (rook components, pawn wall/chain/rank arrays, bishop activity/thresholds, latent-threat per-piece, attack-map SCALE_ATTACK_*), byte-id per group → group-wise PACE screen → assemble → joint-tune (validated) → ONE combined SPRT (placement-magnitude + cut-H + positional-knob survivors). Plan: `~/.claude/plans/all-docs-current-and-woolly-phoenix.md` (PLACEMENT BUNDLE + STAGE B). Memory: [[agentic-eval-tuning-system]].
>
> **▶️ (2026-06-16, latest³ — EVAL-TUNING LOOP RUN FOR REAL: cut-H POC + methodology nailed + phase-2 designed.)** Added finer passed-pawn knobs (`93c8b40`, `PP_*` magnitudes in `getPPIncrement`, byte-id) + prefix theme-match (`c477b75`) + **dispatcher thread-pinning** (`a3378a1`). **METHODOLOGY (hard-won): move-match is non-deterministic under CPU contention** — each process fans out OpenMP+BLAS+TF threads; a few unbounded runs oversubscribe cores (caused a 92% restart) and the fixed-depth search drifts. **FIX baked into the `movematch` dispatcher: pin OMP/OPENBLAS/MKL/NUMEXPR/VECLIB/TF_*=1 and bound concurrency to PHYSICAL cores (8c/16t → ≤6); then 6-wide is byte-identical to single-process** (verified +0). Also: engine move-choice is **OMP-thread-count-dependent** for some positions (order-dependent eval reduction) — pin consistently; pre-pinning numbers aren't comparable to pinned. **RESULT — cut-H POC:** the blunt 4-knob `pp_cut` was a +0.15% WASH that hurt Advancement −54; **isolation found `PP_HORIZ_SUPPORT` (connected-pawn bonus, 225) as the culprit → cutting only it (225→150) = full-suite move-match NET +1.23%** (10 themes up incl Advancement +17/AKPC +32/Center +42/Undermine +57; worst regress Offer −62). **LESSON: knobs INTERACT — always move-match the assembled BUNDLE, never sum isolated cuts.** **POC SPRT DONE (2026-06-17, 2018 games): cut-H = +0.7 ± 17.8 Elo = statistically ZERO** (as predicted for a sub-Elo single knob). Validated the MECHANICS (pipeline ran clean overnight, ~200 games/hr, load steady 6/8) + a non-contradiction (proxy +1.23% → Elo +0.7, signs agree, no gross overfit), but did NOT prove move-match PREDICTS Elo (one ±18 point). cut-H NOT shipped (default 225 stays; fix attribution before shipping passer magnitudes). **Single knobs are sub-Elo → bundling IS the validation experiment.** ▶️ NEXT = first bundle = **SEARCH params** (best validation vehicle: oracle-free proxy WAC/STS-solve+nodes+timed-depth, ready now, jointly-stale headroom, search→Elo more reliably measurable); eval bundles (the gap-closure goal) come after the loop is validated + drift-bank built. Dispatcher tournament case now also thread-pinned. `ScheduleWakeup` doesn't fire outside `/loop` — use the completion notification or `/loop` skill for overnight monitoring. ▶️ PHASE-2 (the needle-mover): **single knobs are sub-Elo → SPSA-style bundles** (inner-loop-vet many knobs → bundle positives → move-match the bundle → SPRT it; calibrate `proxy_elo.csv` at bundle level). **Position-bank expansion:** sample the self-play CORPUS + **SF-distill labels** (SF at FIXED DEPTH, MultiPV top-N + eval-gap partial credit — we have SF static eval in `corpus.csv` but NO best-move labels yet → build a MultiPV annotator extending `annotate.py`); "abstract themes" = position FILTERS; curate toward eval-driven (depth-stable) positions. **Ultimate cheap gate = broad held-out/rotating DRIFT set** (signed-net + churn), banked once at high depth, sampled forever; last gate before SPRT + best `proxy_elo` anchor. Memory: [[agentic-eval-tuning-system]].
>
> **▶️ (2026-06-16, latest² — EVAL-TUNING SYSTEM BUILT: the `/eval-tune` Claude skill + move-match inner loop.)** Eval precision is THE ceiling (4× confirmed), so built the tuning SYSTEM. **Instruments (committed):** `1113c9c` = 4 per-term `SCALE_*` knobs (PASSED_PAWN/LATENT_THREAT/CENTRAL/CAPTURE_GAINS, percent, default 100 byte-id; `total += SCALE_X*term/100` midgame+endgame) + `selfplay/tune_corpus.py` (sample+label: our per-term breakdown + SF static) + `selfplay/tune_fit.py` (Texel fit vs SF-static/result, control split). `36cc20c` = `diagnostics/movematch.py` (themed move-match scorecard over STS: `run`/`diff`/`themes`) + dispatcher `movematch <tag> <themes|all> [KNOBS]` / `movematch_diff <base> <cand>`. **The skill** `.claude/skills/eval-tune/SKILL.md` encodes the loop (diagnose→hypothesize-batch→run→judge→gate→learn) + efficiency rules (batch candidates, pipeline at ~80%, reason at checkpoints, subagents). **TWO DECISIVE LESSONS:** (1) **tune against MOVE-MATCH, not eval-match** — every candidate fit to SF-static/game-result eval *degraded* STS; proof: `SCALE_LATENT_THREAT=144` (eval-fit wanted king-danger up) regressed King-Activity move-match 426→376 in minutes-of-positions (caught by the cheap proxy, no games). (2) **scalar `SCALE_*` is too coarse** — ~1.2 of the 1.32-pawn scatter is in `pieces`/PST; real gains need **finer PST/term-internal knobs** (add them the `env_int` way). **Est ~5–10× fewer games than SPSA+SPRT** once move-match is shown to predict Elo (`proxy_elo.csv` logs scorecard-delta→Elo). ▶️ NEXT = expose finer knobs → run `/eval-tune` for real → gate survivors with one SPRT. Plan: `~/.claude/plans/all-docs-current-and-woolly-phoenix.md`. Memory: [[agentic-eval-tuning-system]].
>
> **▶️ (2026-06-16, latest — PASSER TRUE-PASSED-MASK BUILT → DISCONFIRMED by FEN-probe + control → eval-bound, SHELVED-with-data. Eval precision is THE lever, now 4× confirmed.)** Implemented Part B (true-passed-mask `ENABLE_PASSER_PRUNE_EXEMPT`: `passed_span_white/black[64]` built in `initialize_attack_tables`, externed in cpp_bitboard.h, consumed in the `do_lmr` fold of both `get_score_for_{min,max}imizer`). **Mask works** (fires 9k–17k vs the old rank-only's 825k over-fire) **but is INERT on the move choice.** FEN-probe on the 3 Tal-bot a-pawn-march FENs (black to move, black HAS the passer): identical move ON vs OFF at LIGHTNING (d11), fixed depth 12–18 (TC-capped to d11), and STANDARD (d15). **Decisive control (user-proposed): with ALL reductions OFF (LMP+lazy+LMR+futility+razoring+nullmove) the engine STILL plays quiet `a5b6` not SF's `a4a3`** → pruning EXONERATED, it's **eval-bound** (our leaf eval ≈0/+2.25 for black vs SF black −2.7 on the passer+back-rank bind; the win is a deep positional bind, not an in-horizon promotion). ON-bench (refined): WAC 256 (−5) / +10.6% nodes / STS 51.7% (−0.5%) — cleaner than rank-only but still a regression ⇒ **kept default-off (byte-identical 261/134,429,469), shelved-with-data**; `passed_span_*` retained as a reusable fast-passed-pawn primitive. **A SEARCH fix can't rescue an under-valued eval.** ▶️ NEXT = **Part C: the FEN-based eval-tuning system** (the only lever that moves this) — Texel inner loop + SPRT outer gate, agentic orchestrator; design it in a focused pass. Plan: `~/.claude/plans/all-docs-current-and-woolly-phoenix.md`. Memory: [[collapse-eval-overoptimism]].
>
> **▶️ (2026-06-16, late — COLLAPSE DIAGNOSTIC DONE → eval over-optimism is THE ceiling; passer fix concrete-ready; eval-tuning system = the big next build. Handoff to a fresh context.)** Engine state: **lazy+LMP SHIPPED default-on, +59.2 Elo** (`c623003`); baseline **261 / 134,429,469 / STS 52.2%** (env-off `ENABLE_LMP=0 ENABLE_LAZY_RESORT=0` → 262/260,960,881 byte-exact). **WHY WE LOSE (diagnostic on the 1729-game tournament's 573 losses; tools `selfplay/flip_extract.py` = jsonl eval-drop miner + `selfplay/fen_vs_sf.py` = our-engine-multi-depth vs SF):** 401 flips, **90% MIDGAME**; our eval is **OVER-OPTIMISTIC vs SF (+6/+8 where SF says +4 or negative) and the error GROWS WITH DEPTH** (e.g. game_417 +2.8→+12.8; a calibrated eval wouldn't) → deeper search steers into positions the biased eval over-rewards → engine plays for a fantasy advantage → collapses. Move-match to SF only 62%(LIGHTNING)→69%(STANDARD) — **depth barely helps**. The passer/endgame ~10% slice IS horizon (passer FENs 3/3 SF-match at STANDARD, 1/4 LIGHTNING). **⇒ EVAL PRECISION is the #1 lever — NOT search (depth AMPLIFIES the error); root = the "winning-side over-read" / STS 52-vs-77 gap, now shown as the direct loss cause.** (Trigger: a STANDARD loss to chess.com's 2705 Tal-bot — read ~equal then −7000 on a slow passer march; cold-probe exonerated "stale memory" = intrinsic.) **▶️ NEXT (plan `~/.claude/plans/all-docs-current-and-woolly-phoenix.md`): (B, concrete-ready, the ~10% patch) passer TRUE-passed-mask exemption** — precompute `passed_span_white/black[64]` in `initialize_attack_tables` (mirror `king_ring2`; 3-file forward span per `getPPIncrement`), and in the `do_lmr` fold (search_engine.cpp, both `get_score_for_{min,max}imizer`, replace_all) require `(enemy_pawns & passed_span[mover][to_sq])==0` on top of the rank gate so it fires ONLY on real passers (the ADV=5 rank-proxy OVER-FIRED: 825k/+12% nodes/STS−3.8%). **Test order (user): FEN-probe FIRST at LIGHTNING via `fen_vs_sf` (fires on the a-pawn + lifts move-match off 1/4, low fire-count?) — only THEN the bench ringer (byte-id 262/260,960,881 off; WAC/STS held on).** **(C, the big build — PAUSED, design in a dedicated session, likely a Claude SKILL) FEN-based eval-tuning system:** Texel-style inner loop (corpus = over-read FENs + a CONTROL set, labeled by SF eval / game result; tune eval weights to minimize sigmoid-error; cold-eval workers 6-parallel, seconds not games) + **self-play SPRT outer gate** (`selfplay/sprt.py`, no-regression Elo) — "FEN-tuning for speed, SPRT for truth"; corpus ≈ free (~150k labeled game positions + SF). User's vision = an **agentic orchestrator** queuing HUNDREDS of FENs/round that stands in for the human: judges direction, spots regressions + new/remaining weaknesses with real math feedback, re-targets FENs, iterates. (SPSA stays the eventual joint-optimizer when compute frees up.) Memory: [[collapse-eval-overoptimism]], [[lazy-hybrid-lmp-ordering-safety]], [[fixed-depth-bench-ceiling]].
>
> **▶️ (2026-06-16 — LAZY-HYBRID + LMP SHIPPED default-on: +59.2 Elo. NEXT = collapse/passer diagnostic (gather data, defer fixes).** The overnight tournament settled it: **base vs the gentle lazy+LMP config = +59.2 ±19.2 Elo** (1729 games, LIGHTNING, SF18-adjudicated), mechanism confirmed in-play (`summary.py`: **+0.50 ply at equal time**, 11.62 vs 11.12 on −12.7% nodes). **SHIPPED default-on (`search_engine.h` defaults flipped): `ENABLE_LMP=true` (B3/MAXD3), `ENABLE_LAZY_RESORT=true`, `PROMOTE_TOP_K=2`, `LAZY_RESORT_MIN_CUTOFF_IDX=2`, `RESORT_AFTER_REUSES=1000000` (full-resort off).** **NEW DEFAULT BASELINE: WAC 261 / 134,429,469 nodes / STS 52.2% / EBF 4.20** (`MAX_DEPTH=10 USE_OPENING_BOOK=0 PRESET=LONG_FORMAT`); the OLD **262 / 260,960,881** is the env-off control (`ENABLE_LMP=0 ENABLE_LAZY_RESORT=0`, verified byte-exact). **ANALYTICS** (`paired_analysis.py` + new `selfplay/loss_patterns.py`): fast is better at every imbalance bucket and **wins from behind** (53.5% disfavored) — not opening luck; its losses are **NOT bad starts** (87% started equalish) but **~70% collapse-from-okayish + ~25% squandered-winning in LONG games** (median 89 plies) = the eval/horizon "didn't-see-it-coming" weakness (shared with base, fast just does it less). **⇒ eval/horizon precision is the #1 remaining lever, triple-confirmed (this + the SF ceiling + the game loss).** **Passer-exemption (`ENABLE_PASSER_PRUNE_EXEMPT`, gated default-off) BUILT but FAILED its gate** — ADV=5 rank-proxy over-fires (825k fires, +12% nodes, STS −3.8%); needs the true-passed-pawn-mask refinement. **▶️ NEXT (in progress) = COLLAPSE/PASSER DIAGNOSTIC (data first, fixes deferred):** run the passer danger-point FENs + mine eval-flip FENs from the 1729 games (`flip_extract.py` → `fen_vs_sf.py`, our-engine-multi-depth vs SF), cluster patterns + horizon-vs-eval split, write a report; THEN discuss fixes (passer true-passed mask + whatever the data shows), each gated + speed/accuracy/depth-verified. Plan: `~/.claude/plans/all-docs-current-and-woolly-phoenix.md`. Memory: [[lazy-hybrid-lmp-ordering-safety]], [[fixed-depth-bench-ceiling]].
>
> **▶️ (2026-06-15, late — ORDERING-FOR-PRUNING-SAFETY RESOLVED: lazy-hybrid LOCKED, tournament-pending).** **Phase 1 (gravity/malus × LMP) — DROPPED.** Re-tested WITH the LMP consumer it was supposedly waiting for; STILL net-negative on STS at every `MALUS_DIV` (d2 46.7 / d3 49.5 / d4 47.2 vs **51.5** LMP-alone control; WAC "wins" were d10 noise; B2 push-harder went the WRONG way, 255 vs control 260). **Root cause REFINED (falsifies the old "no consumer" theory): malus pollutes the GLOBAL `[side][from][to]` history table → demotes non-cutting-but-good positional quiets everywhere → shallow/eval-bound search can't absorb it (shows as the STS drop).** **Phase 2 (lazy-hybrid cached-quiet re-sort) BUILT, gated default-off, byte-identical (262 / 260,960,881):** `MoveEntry` += `last_cutoff_index`/`reuse_count`; cutoff index recorded in `updateMoveCacheForBetaCutoff` (gated, so off = no extra find); standalone `score_quiet` helper in `move_gen.h` (mirrors the `generateLegalMovesReordered` quiet lambda — deliberately NOT a refactor, to keep the hot path byte-identical); hook in `buildMoveListFromReordered`'s cache-hit path after the killer/counter swap (re-ranks the quiet tail `[firstNonCapture+indexIncrement..end)` by LIVE `score_quiet`); knobs `ENABLE_LAZY_RESORT`/`PROMOTE_TOP_K`/`RESORT_AFTER_REUSES`/`LAZY_RESORT_MIN_CUTOFF_IDX`. **AGGRESSIVE (full resort + K=4 + idx>1) recovered WAC 255→261 at ~0 nps cost BUT cratered STS to 47.0% — the periodic full re-sort over-concentrates survival on history = the SAME failure mode as malus.** **🔒 GENTLE config WINS (no full resort = `RESORT_AFTER_REUSES` high, `PROMOTE_TOP_K=2`, `LAZY_RESORT_MIN_CUTOFF_IDX=2`): WAC 261 / STS 52.2% (HELD vs 52.3 baseline) / 134.4M nodes (−48.5% vs baseline) — baseline strength on BOTH benches at HALF the nodes.** B2-push (256) & K=3 (251) both worse ⇒ B3 / K=2 / idx>2 is the operating point; the full re-sort is the harmful part (shelved, knob retained). **TIMED-DEPTH CHECK = GREEN LIGHT** (new `wac_timed_depth`/`sts_timed_depth` dispatcher subcommands — lightning, uncapped depth, mean depth excl mate-found): at EQUAL TIME the config is **+0.53 ply (WAC) / +0.57 ply (STS) deeper AND +3 WAC / +2.3% STS stronger** — the fixed-depth −1 WAC inverts to a gain once node savings become depth (the d10-ceiling nuance in action). Also fixed a latent `tactical_test.py` bug (eval/nodes CSV columns blank — Python-print stdout buffer not flushed before the fd-capture read; mate sentinel = **9999995**). **▶️ NEXT = OVERNIGHT TOURNAMENT** base vs the locked config — `tournament <mins> 'ENABLE_LMP=1 LMP_BASE=3 LMP_MAX_DEPTH=3 ENABLE_LAZY_RESORT=1 PROMOTE_TOP_K=2 LAZY_RESORT_MIN_CUTOFF_IDX=2 RESORT_AFTER_REUSES=1000000'` → Elo + per-arm depth → flip defaults default-on only on non-regression. Then SEE-pruning (next absent EBF lever) → futility-margin + decay-knob unification → SPSA cluster. **⚠️ Phase-2 engine code + the dispatcher/`tactical_test` tooling are currently UNCOMMITTED (gated default-off = byte-identical, safe to commit).** Memory: [[lazy-hybrid-lmp-ordering-safety]], [[fixed-depth-bench-ceiling]].
>
> **▶️ (2026-06-15) — SF18 is now the default arbiter/reference + PINNED fixed-depth CEILING measured.** `stockfish/` → **Stockfish 18** (SF16.1 archived at `stockfish_16/`; same binary name so all hardcoded paths resolve unchanged; python-chess `popen_uci` + `UCI_LimitStrength`/`UCI_Elo` verified working). **Ceiling reference (`diagnostics/sf_ceiling.py`, SF18 single-thread/64MB/no book-TB, PINNED):** **WAC d10 = 203/300 @ ~4,751 nodes/pos** (vs us 262/300 @ ~870k — at a FIXED depth we out-solve SF on tactics purely by ~183× width; SF's EBF~2 tree is blind to ~60 tactics ours catches → "depth 10" is NOT comparable across engines, and fixed-depth WAC FLATTERS us); SF d20 achievable ceiling = **280/300** (77 of 97 d10 misses are depth-gated; ~20 SF still won't play even deep = the suite's answer-disagreement floor). **STS d10 = 77.4%** (vs us 52.2% @ ~70× the nodes) ⇒ **positional play is EVAL-bound, not depth-bound — eval precision is the #1 remaining ceiling, and pruning-aggression is downstream of eval quality** (SF prunes hyper-hard *because* its eval is trustworthy; on our noisy eval, hyper-pruning prunes the wrong lines — the aggressive-lazy STS crater confirms it). **Strategic read: after the pruning work lands, the highest-leverage lane is SPSA-tuning our (already feature-rich) eval, not adding features.** Memory [[fixed-depth-bench-ceiling]]. For a future fixed-strength opponent: `UCI_LimitStrength`+`UCI_Elo` (1320–3190) or fixed-nodes (more reproducible); needs an SF-as-player arm in the harness (today SF is arbiter-only).
>
> **▶️ (2026-06-15, EBF sprint) — LMP BUILT + committed gated-off (`8eaf80e`); NEXT = ORDERING-FOR-PRUNING-SAFETY (then race LMP).** EBF (4.65 @d10 vs ~2 optimal) is now the lever; cheaper eval (shipped bundle) makes pruning affordable. **Cutoff move-index histogram (committed `be5a1b1`; `g_cutoff_histogram` + dispatcher `wac` HIST line): 96% of cutoffs at move 0/1, m8+ 1.3% → engine is PRUNING-bound, not ordering-bound.** **LMP (`ENABLE_LMP`, default-off, byte-id 262/260,960,881):** in `get_score_for_{minimizer,maximizer}` reusing the `do_lmr` eligibility (never prunes captures/checks/promos/killers/in-check); knobs `LMP_MAX_DEPTH`/`LMP_BASE`/`LMP_SCALE`; prune = early `return ±9999999` (caller unmakes). **Bench = powerful but over-prunes at fixed depth:** aggressive `B3/MAXD3` −47% nodes/EBF 4.65→4.20/WAC 255(−7)/STS 51.5%; conservative `B12/MAXD3` −25%/259(−3)/50.2%; **NON-MONOTONIC at d10 → fixed-depth can't pick the point, self-play is the arbiter** (depth gain ~+0.45 ply aggressive / ~+0.2 conservative; needs an OVERNIGHT race — ~1hr is too noisy). **METHODOLOGY (user): WAC-at-fixed-depth is a GUARDRAIL, not the verdict** — lost solves are fine if the node-cut→depth→Elo wins in timed play (Δdepth ≈ ln(1/ratio)/ln(EBF), logarithmic). **▶️ NEXT = make LMP safe enough to push aggressive via better quiet ordering** (keep good quiets above the LMP threshold → recover the WAC loss → push LMP harder = the virtuous cycle). LMP is the **consumer** the shelved [[ordering-history-design-ideas]] gravity/malus + lazy-hybrid lane lacked (both shelve-reasons now inverted). **Synergy metric = "WAC solves recovered at aggressive `B3/MAXD3`".** **Phase 1 (FREE, no rebuild — knobs exist):** gravity/malus × LMP fixed-depth (`SATURATION+MALUS` vs `MALUS`-on-simple × `MALUS_DIV` × {B3/MAXD3,B12/MAXD3}). **Phase 2 (build):** lazy-hybrid cached-quiet re-sort — hook `buildMoveListFromReordered` (cache-hit ~search_engine.cpp:4317), `MoveEntry` += `last_cutoff_index`/`reuse_count` (the commented-out `num_beta_reorders`), record cutoff index in `updateMoveCacheForBetaCutoff`, extract `score_quiet` from move_gen.h:707; **gate top-K promote on `last_cutoff_index>1` (cutoff NOT in first 2), full re-sort on reuse-without-cutoff, pin cutoff/killer/counter at front**; knobs `ENABLE_LAZY_RESORT`/`PROMOTE_TOP_K`/`RESORT_AFTER_REUSES`/`LAZY_RESORT_MIN_CUTOFF_IDX`. **Phase 3:** combine all 3 → one overnight tournament. **FLAG (lazy-hybrid risks):** re-score cost on the hot cache path; `last_cutoff_index` is a noisy prior-window signal; compose with the existing killer/counter swap; **deepest risk = ordering may already be good enough that LMP-pruned tactics are history-invisible → fallback = LMP-side exemptions (high-history/passed-pawn/near-promotion quiets) or ship conservative LMP.** Tooling: `selfplay/overnight_runner.sh` dispatcher (`build`/`wac`[+EBF/HIST]/`wac_timed`/`sts`/`tournament`/`result`/`clock`/`ps`) + scoped WSL allow in `.claude/settings.local.json` (gitignored) — **invoke inline `wsl … bash '…/overnight_runner.sh' <sub>` (a leading `VAR=…;` breaks the prefix match → prompts)**. Plan: `~/.claude/plans/all-docs-current-and-woolly-phoenix.md`. Memory: [[ebf-lmp-ordering-sequence]]. After ordering+LMP: SEE-pruning (ABSENT) → futility-margin knobs/SPSA → SPSA+SPRT loop.
>
> **▶️ (2026-06-15) — EVAL-SPEED BUNDLE SHIPPED default-on + committed (`535513f`); pivot to the EBF sprint.** Rook mobility surrogate (`ENABLE_CHEAP_ROOK_MOBILITY`) + lossless attack-layer caches (`ENABLE_ATTACK_LAYER_CACHE` endgame + `ENABLE_ATTACK_LAYER_CACHE_MIDGAME` split white/black half-caches) all default-on. **+8.6% nps / −3.8% wall @d10; self-play +11.2 ±20.7 Elo (1487 LIGHTNING games, positive lean, no regression).** **NEW d10 control: WAC 262/300, 260,960,881 nodes, STS 52.3%** (old 260/249,966,786 recoverable via the three knobs `=0`; that rook-on 260,960,881 is the new byte-identity gate — caches are lossless). **KEY FINDING: cheap-mobility does NOT generalize past the rook** — `ENABLE_CHEAP_QUEEN_MOBILITY` (STS −66; the don't-count-enemy-defended-squares filter is LOAD-BEARING for the queen) + `ENABLE_CHEAP_KNIGHT_MOBILITY` (WAC −3 / +11% node bloat; knight term already cheap → only disruption) built/benched/REJECTED, retained gated default-off. Bishop skipped (its activity cost is irreducible side-effects, not a skippable filter). **The caches work because the endgame king-danger layer is a pure function of the two king squares; midgame adds king-zone occupancy/pawns → split half-caches keyed via `king_ring2[64]`.** **Tooling shipped: `selfplay/overnight_runner.sh`** — a vetted dispatcher (`build`/`wac`/`wac_timed`/`sts`/`tournament`/`result`/`clock`/`ps`) that is the ONLY thing a scoped WSL allow (`.claude/settings.local.json`, gitignored) lets run unattended → no permission prompts; **invoke inline `wsl … bash '…/overnight_runner.sh' <sub>` (a leading `VAR=…;` breaks the prefix match → prompts).** **▶️ NEXT = EBF SPRINT** (EBF ~4.6 vs ~2 optimal = the exponential lever; cheaper eval now makes eval-cost-gated pruning affordable): **(1) cutoff move-index histogram diagnostic FIRST** (reuse `g_fh_total`/`g_fh_first` at the two `if (beta <= alpha)` cutoff points in minimizer/maximizer → decides pruning vs secondary-ordering emphasis); **(2) LMP + (3) SEE-pruning — both confirmed ABSENT in the main search** (`see()` defined cpp_bitboard.h, only used for ordering + qsearch filter today); (4) re-test [[improving-heuristic-shelved]] (`ENABLE_IMPROVING`, lost −0.18 ply when eval costlier, now ~8% cheaper); (5) search profiler (reuse the `__rdtsc` framework) + lazy move-sort (`generateLegalMovesReordered` scores+sorts ALL moves up front). **Behavioral gate (NOT byte-identity): nodes DOWN while WAC solves + STS HOLD, then self-play.** Plan: `~/.claude/plans/all-docs-current-and-woolly-phoenix.md`. Memory: [[eval-speed-bundle-shipped]].
>
> **▶️ (2026-06-14, late³) — PIVOTED TO EVAL-SPEED; re-profiled → target = ROOKS.** Eval-correctness lane is exhausted (rescan arc closed), so the pivot is eval-SPEED (compounds: cheaper eval → more nps + affordable prune→verify → lower EBF → depth). **Measured EBF mean 4.64 @d10 / ~3.4 @STANDARD vs ~2 optimal** (`ebf=nodes^(1/depth)`, search_engine.cpp:1017), FMC 92.1% (NOTE: FMC is *move-1* ordering only — near-tapped; *secondary-move* ordering is a separate, unmeasured EBF lever → queued "cutoff move-index histogram" diagnostic). **Re-profiled** (`eval_profile.py --tag correctness_vs_base --reps 2000 --cap 400`, clean machine): **cheap-bishop win CONFIRMED** (BISHOP_COLOUR 33%→~1%); **#1 hotspot = ROOKS (22% early-mid → 33–34% mid/end, 543 cyc/call — most expensive per-call)**, `evaluate_rooks_{midgame,endgame}` (per-rook file-scan loop + `attackingLayer` reads); **#2 = ATTACK_LAYER** (18% late-end, one-entry-memo candidate); #3 CAPTURE_GAINS (~10–14%). **▶️ NEXT = lazy-eval ROOKS** (dissect → cheaper-loop/early-exit/memo/surrogate, gated default-off, byte-identical WAC 249,966,786/260, bench + self-play-gated; cheap-bishop template). Plan: `~/.claude/plans/handoff-for-the-glistening-token.md` (NEXT MOVE section). Profiler runs in ~min → re-profile after each change.
>
> **▶️ (2026-06-14, late²) — DEAD-PIN (#2) DELETED (eval-side pin feature removed; BYTE-IDENTICAL). Rescan #2 CLOSED.** Resolved the dead `get_relevant_pin` (ANDed disjoint masks → always 0 → inert) via measure-then-delete. **Findings:** reviving it (`ENABLE_PIN_FIX`) is play-NEGATIVE — full (mobility-gating + flat ±150): **WAC +9.5% nodes / STS −67**; mobility-only (flat dropped, the isolation sub-test): still **+4.9% nodes / STS −54** (+1 WAC solve). It DOES improve static-eval accuracy on true-pin positions (~0.24–0.30p toward SF, 7/10 fired) — but that **doesn't convert**: it's REDUNDANT with the search's correct LMR pin-handling (`relevant_pin_exists`, which is LIVE + correct + LEFT UNTOUCHED) and perturbs move choice (accuracy≠strength). **DELETED:** `get_relevant_pin` + `relevant_pins` plumbing (6 evaluator signatures + call sites) + 12 mobility-gating uses + 6 flat-±150 lines + the `ENABLE_PIN_FIX` knob/wiring. **BYTE-IDENTICAL: WAC stays 260/300, 249,966,786 nodes** (feature was inert) + a tiny per-eval king-setup save. Committed. **▶️ NEXT = eval-SPEED re-profile** (`PROFILE_EVAL=1`, clean machine). Plan: `~/.claude/plans/handoff-for-the-glistening-token.md`.
>
> **▶️ (2026-06-14, late) — SEARCH LATENT-BUGS SPRINT (rescan #8): shipped the TT depth-honesty fix; the other two resolved as NON-issues. Bench-decided, NO tournament.** Built 4 gated knobs (byte-identical off, verified WAC 255,372,592/259), benched each on WAC nodes/solves + STS d10. **`ENABLE_TT_DEPTH_FIX` → SHIPPED DEFAULT-ON (the keeper):** `reorder_legal_moves`' pre-pass searches `depth_limit-1` but stored `depth_limit` in the TT (+1 over-trust); storing the honest depth WON on every axis — **WAC 259→260 solves, 255,372,592→249,966,786 nodes (−2.1%), STS 50.6→51.7% (+33)**. **NEW d10 CONTROL BASELINE: WAC 260/300, 249,966,786 nodes** (`MAX_DEPTH=10 USE_OPENING_BOOK=0 PRESET=LONG_FORMAT`). **`ENABLE_QPREC_PHASE_GATE` → KEPT OFF (documented negative):** restoring the intended phase-gating (`use_q_precautions` forced-true at search:845 overrides the phase branches) tested WORSE (−3 solves, +9.1% nodes) → the accidental always-on is genuinely better. **null-move 3-vs-4 → NOT a bug (PARITY artifact):** minimizer sits at odd `cur_depth`, maximizer at even, so `>=3`/`>=4` are symmetric in effect — `NULLMOVE_CURDEPTH_MAXI=3` byte-identical (no maximizer node at cur_depth 3), `MINI=4` worse (−1 solve, +13% nodes); defaults 3/4 optimal, knobs retained for future MAXI=2 tuning. **All 4 knobs committed** (TT_DEPTH on; qprec off; null-move 3/4). **Adversarial-rescan #8 search latent-bugs CLOSED.** Remaining rescan loose ends (all low-pri, deferred): A2b TT draw-cache, dead-pin measure-or-delete, ROOK_ENDGAME_CAP/ROOK_RANKWIN tuning knobs, #7b TTEntry shrink. **▶️ NEXT = eval-SPEED re-profile** (`PROFILE_EVAL=1`, clean machine → lazy the top hotspot). Plan: `~/.claude/plans/handoff-for-the-glistening-token.md`.
>
> **▶️ (2026-06-14, evening) — capgain-alone A/B = NEUTRAL; causal check shows capgain is ACTIVE-but-net-neutral → eval-correctness lane confirmed EXHAUSTED; PIVOT to eval-SPEED.** `capgain_vs_base` (562 valid games, 92 voided by a mid-run rebuild cascade — lesson logged): **+230 −245 =87 = 48.7%, Elo −9.3 ±33.7** (CI [−38,+19], inconclusive; as White 52.7% / as Black 44.7%). **NOT the −30 the bundle-deduction predicted** → the −32 bundle was a combination, not capgain. **Causal check (`diagnostics/capgain_causal.py`, 1500 tournament positions, off vs on builds): capgain FIRES in 17.3%, mean |eval shift| 0.94p (median 0.25, max 4.59), ≥0.5p in 6.5%** — i.e. NOT rare. So a moderately-active, mirror-CORRECT eval change produced ~zero measurable strength → **strength is not eval-correctness-bound here; the eval-correctness lane is exhausted** (like move-ordering earlier). The 4.59p single-term tail flags the full rank-bonus magnitude as too hot → **"scale the capgain credit" is a now-better-motivated LOW-priority backlog item.** **DECISION (user): SHIP capgain default-on** — it's a genuine mirror-verified bug, the A/B shows no significant harm, and a correct eval is a sounder foundation (ship-correctness-when-neutral). **`ENABLE_CAPGAIN_PAWN_FIX` flipped default-on + committed.** **NEW d10 CONTROL BASELINE (capgain-on): WAC 259/300, 255,372,592 nodes** (`MAX_DEPTH=10 USE_OPENING_BOOK=0 PRESET=LONG_FORMAT`) — same solves, −0.45% nodes vs the old capgain-off 256,518,458 (neutral-or-better on the bench too). Also committed default-off (byte-identical) + retained: `ENABLE_KNIGHT_MOB_SYM_UP`/`ENABLE_ROOK_DBLCOUNT_SYM_UP` knobs, the `ae_*` advanced-endgame decomposition fields, and diagnostics `capgain_causal.py`/`scatter_decomp.py`/`symup_accuracy.py`. **▶️ NEXT = eval-SPEED (re-profile `PROFILE_EVAL=1` on a CLEAN machine → validate cheap-bishop → make the top hotspot lazy → reopens shelved reductions/improving + buys depth; bench-provable, no 10k-game dependency) + ordering/NPS/FMC (lazy-hybrid quiet reorder, staged movegen, qsearch nps).** Plan: `~/.claude/plans/handoff-for-the-glistening-token.md`.
>
> **▶️ (2026-06-14, daytime) — DISSECTED the −32 regression: capgain is a CONFIRMED-CORRECT ~2p bug fix (likely the −32 driver); the colour asymmetries are rounding-error (~0.05p) and symmetrize-up is accuracy-NEUTRAL.** Probed via `eval_breakdown.py` + a new `diagnostics/symup_accuracy.py` (SF-divergence proxy). **(1) CAPGAIN (`ENABLE_CAPGAIN_PAWN_FIX`): GENUINELY CORRECT.** Mirror pair: Black-takes-advanced-White-pawn reads **+0.07 OFF (≈0, wrong) → −2.06 ON**, exactly mirroring White's unchanged +2.06. `pawn_rank_bonuses` is **signed by colour** (≈−1060 for the advanced White pawn) so baseline computes Black's gain = 1000+(−1060) = −60 ≈ 0 — it nearly ERASES Black's credit for capturing an advanced White pawn. Swings ~2p when it fires. **(2) SYM-UP knobs built & verified** (`ENABLE_KNIGHT_MOB_SYM_UP` raises white knight base 10→15; `ENABLE_ROOK_DBLCOUNT_SYM_UP` adds white's missing rook-behind term) — default-off **byte-identical (WAC 256,518,458/259)**; ON-probe confirms each raises White exactly to Black (knight +4.28/−4.28, rook +3.99/−3.99). But the **accuracy proxy (vs SF_static, 300 endgame game positions) = NEUTRAL**: fired n=114, mean|err| base 2.706 → symup 2.705 (Δ−0.001), 55% toward-SF; corrections ~0.05p (knight-dominated; the big rook double-count is geometrically RARE in real games). → **symup not worth a dedicated overnight.** **(3) THE REAL LEVER (proxy side-finding):** baseline over-reads won endgames by **+9 to +13p vs SF** (winning-side-hot scatter), ~100× any asymmetry knob — eval precision is the prize. **DEDUCTION:** knight/rook are ~0.05p so the −32 bundle was almost certainly **capgain** (~2p, was in it) → a provably-correct fix may be Elo-NEGATIVE, OR (user) the rank-bonus **magnitude needs retuning** (scale the credit), not reverting. **NEXT = capgain-alone vs base self-play** (the static proxy under-samples capgain because it shifts search-leaf pruning); NOT yet launched (user ~9–10h from sleep). All new knobs committed-nowhere-yet / uncommitted, default-off. Plan: `~/.claude/plans/handoff-for-the-glistening-token.md`.
>
> **▶️ (2026-06-14) — CORRECTNESS-BATCH A/B RAN → SIGNIFICANT REGRESSION; all three fixes STAY gated default-off (SHELVED with data).** Overnight LIGHTNING A/B, the 3 "pure" eval-correctness fixes bundled (`ENABLE_CAPGAIN_PAWN_FIX` + `ENABLE_ROOK_DBLCOUNT_FIX` + `ENABLE_KNIGHT_MOB_FIX`) vs bare baseline, color-balanced, draw-adjudicated, 1610 games in 8h: **correctness +588 −737 =285 = 45.4%, Elo −32.2 ±19.9** (95% CI ~[−52, −12], **excludes zero → real regression, not noise**). **Pure move-quality, NOT speed:** per-arm depth/nodes/knps essentially identical (base 11.06 / 659k / 554knps vs correctness 11.04 / 653k / 549knps) — same depth, worse moves. **Color-asymmetric: as White −17.8 Elo, as Black −46.9 Elo** → points straight at the Black-specific changes, and `ENABLE_CAPGAIN_PAWN_FIX` IS the Black-branch sign flip in `approximate_capture_gains` → **prime suspect; the "sign bug" may not have been a bug** (the original `+` plays better; the rest of the eval is co-adapted to it). **DECISION: keep all three default-off (already are — engine you play is unaffected, committed baseline `1f7669a` remains strongest). Batch SHELVED with data.** Same family of lesson as improving/gravity: "correct by construction" ≠ "stronger in play." **EXCLUDED from this run (still open): `ENABLE_ROOK_ENDGAME_CAP` (its own value-sweep) + `ENABLE_PIN_FIX` (measure-or-delete).** Optional isolation (which of the 3 is the villain) = one full ~1600-game run each = a night apiece, LOW ROI given these aren't strength levers. **NEXT: eval-precision (the NNUE-static gap) + Phase D search — NOT chasing the rescan eval-correctness tail.**
>
> **▶️ (2026-06-13) — BATCH 1 + improving sign fix COMMITTED gated/inert default-off (`60770b0`); improving RE-TESTED → stays shelved.** All default-off, byte-identical (`MAX_DEPTH=10 USE_OPENING_BOOK=0 PRESET=LONG_FORMAT` = 256,518,458). • **`ENABLE_ROOK_ENDGAME_CAP`** (+`ROOK_ENDGAME_CAP`=300): `evaluate_rooks_endgame` applies `rookIncrement` UNCAPPED vs `evaluate_rooks_midgame`'s `std::min(...,300)`; clamps it (magnitude fix, endgame rook over-valuation). On = +5.2% nodes / −1 WAC @d10 → self-play candidate. • **`ENABLE_ROOK_RANKWIN_FIX`** (midgame rook own-pawn rank-window white `<5` vs black `>4` mirror) → **PARKED**: `eval_symmetry` couldn't confirm a residual drop (motif too rare + loop break-coupling). • **`static_eval_for_improving` SIGN FIX** (the #5 bug): `get_board_evaluation` flips by `Config::side_to_play` before caching, so the cache-HIT path returned the flipped value while the MISS path returned raw `placement_and_piece_eval` → corrupted the improving comparison. Fixed (miss path now flips too); inert by default. **Re-test on the now-correct signal: WAC d10 −9.5% nodes / +1 solve (a flip from the buggy +8–12% blowup), and a 577-game LIGHTNING A/B gave +0.095 ply DEPTH but −15 ±26 Elo = DEEPER-BUT-NOT-STRONGER → `ENABLE_IMPROVING` stays shelved.** **KEY LESSON: a reduction knob's "+depth" is speculative pruning, NOT free speed — the score is the verdict, depth misleads (it reached more nominal depth by cutting nodes that mattered).** The sign fix is kept as correctness; the shelved-improving verdict is now SOUND (was confounded by the bug). **#7 CH `[piece][to]` reindex — ATTEMPTED & REJECTED (2026-06-13):** implemented across ~30 sites (clean build), but worse on every axis vs HEAD (d10 WAC LONG_FORMAT: nps −7.4% [524k vs 566k], nodes +3.6%, WAC 256 vs 259, wall +11.8%) → REVERTED. The 134MB-cold-cache theory is refuted (search only touches the few `[prev][cur]` entries that occur) + `ch_slot`'s per-index piece-type compute added hot-loop cost + `[piece][to]` lost `[from][to]` counter-move precision. Don't re-attempt; footprint is harmless. **#8 NEAR-NO-OPS INVESTIGATED (2026-06-13) — mostly NOT no-ops:** only the unreachable **root killer/history update** (after the `return best_score` at the root beta-cutoff in `alpha_beta`) was a true no-op — compiler-dead + near-useless at root (ply-0 killers unread) → **deleted (cosmetic, byte-identical).** The other three are **behavioral latent-bugs, unknown sign** (NOT free cleanups): (a) **`use_q_precautions` forced `= true` unconditionally** masks intended phase-gating — the phase logic meant to skip shallow (`depth_limit<6`) qsearch ONLY in endgames (`phase_score>=96`), but always-on makes ALL phases skip it; restoring phase-gating would add qsearch to early midgame iterations (changes d10 nodes). (b) **TT-depth +1 inflation** in `reorder_legal_moves` (stores `depth_limit`, searched `depth_limit-1`) → TT over-trust. (c) **null-move `cur_depth>=3` (minimizer) vs `>=4` (maximizer)** — a small mirror asymmetry (a position null-prunes at depth 3 but its colour-mirror doesn't), unknown sign. → All 3 documented as latent-bugs for a FUTURE search-knob A/B (kept separate from the eval-correctness overnight so signals don't mix). **NEXT: overnight correctness-batch A/B (Phase B + `ENABLE_ROOK_ENDGAME_CAP` vs baseline) — user launches before sleep; then `ENABLE_PIN_FIX` measure-or-delete, TTEntry shrink (#7b), Phase D.**
>
> **▶️ (2026-06-13) — PHASE B endgame colour-asymmetry fixes COMMITTED gated default-off (`45e8839`).** Four `grep`-able knobs, all byte-identical when off: **`ENABLE_CAPGAIN_PAWN_FIX`** (capture-gains `pawn_rank_bonuses` sign in `approximate_capture_gains`' black branch), **`ENABLE_ROOK_DBLCOUNT_FIX`** (extra unconditional `rookIncrement` add in `evaluate_rooks_endgame`'s black "behind a white pawn" branch — no white mirror), **`ENABLE_KNIGHT_MOB_FIX`** (`evaluate_knights_endgame` mobility bonus 15 black vs 10 white), **`ENABLE_PIN_FIX`** (`get_relevant_pin` ANDed disjoint masks = always 0 → dead; fix ORs them). Symmetry-probe (`eval_symmetry.py`) confirmed the colour fixes shrink their mirror residual (`capture_gains` mean −0.128 → +0.011, ~−91%; `pieces` +0.039 → +0.026). **STRENGTH UNVERIFIED** — these are small endgame-motif correctness fixes; a self-play A/B is STAGED (color-batch vs `ENABLE_PIN_FIX` isolation) but NOT run (user defers tournaments). ⚠️ **METHODOLOGY FIX (user-diagnosed): byte-identity benches MUST pin `PRESET=LONG_FORMAT`.** The default `Config::ACTIVE = STANDARD` (`TIME_LIMIT` 45s) lets hard d10 positions time-abort at a wall-clock-dependent point → node count drifts under machine load (saw the same source read 256,518,458 idle vs 260,131,069 loaded). Canonical: `MAX_DEPTH=10 USE_OPENING_BOOK=0 PRESET=LONG_FORMAT` → HEAD == Phase-B-all-off == **256,518,458 EXACTLY** (LONG_FORMAT's 600s never triggers at d10; the MAX_DEPTH cap keeps it fast). Memory `bench-determinism-book-flakiness`. **Also (convention, user): docs/memory cite stable SYMBOLS not line numbers** (memory `docs-cite-symbols-not-line-numbers`). **NEXT (plan `~/.claude/plans/handoff-for-the-glistening-token.md`): Batch 1 endgame-asymmetry tail — `ENABLE_ROOK_ENDGAME_CAP` (uncapped endgame `rookIncrement`) + `ENABLE_ROOK_RANKWIN_FIX` (midgame rook rank-window mirror) — then Phase C (#5 improving sign bug, #7 CH `[piece][to]` reindex).**
>
> **▶️ (2026-06-13) — BLITZ CONFIRMATION: current = +201 ±28 Elo over old CE (545 games, BLITZ, draw-adjudicated; 347W/135D/63L = 76.1%; hourly +155→186→200→194→191→186→194→201).** **CLINCHER — per-arm mean search depth is now DEAD EQUAL (current 12.38 vs old 12.37)** yet current still wins by +201, *slightly larger* than the lightning margin → the whole gain is BETTER-MOVES-AT-EQUAL-DEPTH, NOT a speed/depth artifact (current's 2.3× nps buys breadth — 1.95M vs 902k nodes — at the SAME depth). Old CE put on BLITZ via its `Config::ACTIVE = &Configs::BLITZ` default + rebuild (both BLITZ structs verified identical, `TIME_LIMIT=3.5`). **NET across both TCs: the optimization arc = a genuine ~+180–200 Elo gain, confirmed at two independent controls with matched depth.** ⚠️ **REUSABLE GOTCHA for `.so`-vs-`.so`: old CE has NO `getenv("PRESET")` reader — it ONLY honors its compile-time `Config::ACTIVE` default, so EACH preset needs a `search_engine.h` ACTIVE edit + rebuild of old CE (the `--preset` flag governs current only). Permanent fix if we keep using old CE = port the ~12-line getenv block into it (strength-neutral plumbing).**
>
> **▶️ (2026-06-12) — CUMULATIVE-IMPROVEMENT TOURNAMENT RAN: current `.so` = +176 ±21 Elo over the pre-optimization old CE (870 games, LIGHTNING, draw-adjudicated).** Final tally (current POV) 528W / 221D / 121L = 73.4% score; hourly trace +218→+183→+172→+174→+176 (clean convergence). **KEY TELEMETRY (`summary.py` per-arm): mean search depth is ESSENTIALLY EQUAL (current 10.84 vs old 10.74)** — current runs ~2.3× nps (529 vs 232 knps) and ~2× nodes in *less* time, but spends it on breadth/thoroughness at the SAME depth, not extra plies. So **+176 Elo is overwhelmingly better-moves-at-equal-depth (SEE fix + eval correctness + cheap-bishop accuracy + less-lossy pruning), NOT "sees deeper"** — a stronger endorsement of the correctness/eval work than a pure speed win. ⚠️ **PRESET GOTCHA (fixed): old CE PREDATES the `getenv("PRESET")` plumbing** — it ignores the harness `PRESET=LIGHTNING` env and runs its compile-time `Config::ACTIVE` default (was LONG_FORMAT → 8-min games on the first smoke). Fix = user set old CE's `Config::ACTIVE = &Configs::LIGHTNING` default + **rebuild** (a header edit needs recompile). Verified fair: both LIGHTNING structs IDENTICAL (`TIME_LIMIT=1.0`, same per-move times), confirmed both engines at ~1.5s/move in play. **`.so`-vs-`.so` harness committed (`394606a`); old-CE `.so` built (NOT git-tracked — left out by design).** **A3 (qsearch quiet-check `ENABLE_QCHECK_DEPTH0`+`ENABLE_QCHECK_MASK`) — SHELVED off:** default-off byte-identical (256,518,458 ✓), both-gates-ON = +15.6% nodes @d10 (the `qDepth==0` restriction backfires). MASK-alone left UNTESTED as a low-priority tidy-up, not strength work. **NEXT: Phase B — endgame colour asymmetries (capture-gains signed `pawn_rank_bonuses` fix FIRST), then `get_relevant_pin` fix-and-measure.**
>
> **▶️ ORDER OF OPERATIONS (2026-06-11/12, SUPERSEDES the prior backlog — an adversarial cross-model rescan surfaced PURE BUGS that come first; memory [[adversarial-rescan-2026-06-11]]).** Profiler committed `0ad1485`; bishop surrogate `dd8486e`. **SHIPPED & DEFAULT-ON (commits `3354ee9` A1, `4a27362` SEE-gated, `85c26f0` flips+A2a):**
> • **A1** — `has_any_legal_move` leaf early-exit, byte-identical (WAC 267,284,369) + **~7% faster** wall-clock @d10. Permanent.
> • **SEE FIX** (`ENABLE_SEE_FIX`, default-on) — corrected `see()`: recompute attackers per-iter (exact x-ray) + true-piece-type LVA. Harness-proven (`see==ref` 0.00%, was 2.16%); **self-play +18.2 ±29.3 Elo / no regression**; −0.02 ply. Fixes ordering + qsearch filter + capture_gains. (Pulled fwd from Phase B3.)
> • **CHEAP-BISHOP** (`ENABLE_CHEAP_BISHOP_COMPLEX`, default-on) — ~25% cheaper midgame eval; **self-play +2.7 ±28.8 (no reg)**, depth **+0.13 ply midgame / ~0 endgame** (matched, `diagnostics/depth_by_phase.py`). Flipped on for the compounding speed.
> • **A2a** — eval-cache 0-sentinel fix (`accessCacheNew`→bool+outparam); byte-identical now SEE-on bypasses the only stale-`square_values` consumer.
> **NEW d10 control (USE_OPENING_BOOK=0, all default-on): WAC 259/300, 256,518,458 nodes (−4.0%), STS 1463.** (STS dip vs cheap-bishop-only 1520 = deterministic SEE-ordering churn; the +18.2-Elo self-play is the gate, not d10 STS.) **REMAINING PHASE A: A2b (TT draw-cache, gated, repetition-hazard check first) + A3 (qsearch quiet-check qDepth==0 + mask, gated).**
> **Phase A — byte-identical / near-byte-identical speed (provable on WAC nodes, do first):** (A1) leaf-stalemate `exists-any-legal-move` early-exit — `is_stalemate` runs a FULL movegen per fresh leaf, profiler-INVISIBLE ~15-30% leaf cost (in `is_stalemate` / `get_board_evaluation`). (A2) zero-sentinel cache — TT refuses `score==0` (`addToSearchEvalCache`) + eval-cache callers use `!=0` as miss → **draws never cached**; fix via the existing `valid` flag (byte-identical except draws now served). (A3) `buildNoisyMoveList` quiet-check (the one #8 win expected at lightning) — restrict quiet-check inclusion to `qDepth==0` + replace the per-quiet-move simulate+`is_check` with the direct-check mask test from the `ENABLE_CHECK_ORDER` code; behavioral, shrinks the q-tree (nodes↓ too).
> **Phase B — correctness, behavioral, bench one-at-a-time, aimed at the #1 late-endgame scatter:** (B1) endgame COLOUR asymmetries — signed `pawn_rank_bonuses` in `approximate_capture_gains` (CONFIRMED) then knight 10/15, rook double-count, rook rank-window; re-run `eval_symmetry.py` on an ENDGAME-phase corpus before+after (the old "0.000 EXACTLY" under-covered endgame branches). (B2) `get_relevant_pin` dead (`& bishops&knights&rooks`=0) → FIX (`|`) + measure if the live pin feature earns its keep vs DELETE for the small speed. (B3) SEE — build a brute-force exchange self-check FIRST, then fix stale-`square_values` attacker pick + one-sided refresh (in `see()`); load-bearing (ordering + qsearch filter + capture_gains).
> **Phase C — re-tests UNBLOCKED by A+B:** (C1) improving heuristic — fix the sign bug (`static_eval_for_improving` hit=flipped/miss=raw) → re-test with the now-cheaper eval (the −0.18 verdict is CONFOUNDED). (C2) CH `[piece][to]` reindex (134MB→~12KB) → re-evaluate CH-A + 2-ply CH on a fair table. (C3) flip `ENABLE_CHEAP_BISHOP_COMPLEX` default-on after self-play.
> **Phase D — bigger levers (per the standing roadmap, AFTER the above):** aggressive pruning + verify (LMP = the cheapest history-consumer, unblocks the parked gravity/malus; SEE-pruning; softer null-move re-test) → lazy/staged move-gen (the nps win; subsumes A1 + the 16×-movegen-per-node cost) → SPSA+SPRT tuning loop → eval-precision work. Move-ordering experiments (gravity/malus, lazy-hybrid quiet reorder) LAST. **Unifying thesis: cheaper eval lowers the cost of the "speculatively-prune → verify-if-wrong" pattern, which is what ALL aggressive pruning is — so eval-speed (A) fuels search (D).**
> **DEFERRED DESIGN DEBT (not a hotfix):** the qsearch blanket delta-prune is coupled to capture_gains-baked-into-stand-pat + the depth<6 raw-eval leaves — touching it piecemeal regresses. It's the largest qsearch strength item but belongs to an eventual **stand-pat / qsearch redesign** (pure static stand-pat + per-capture delta pruning), which would also remove the capture_gains double-count. **SEE confirmed empirically broken (`diagnostics/see_selfcheck.cpp`: 2.16% of captures wrong, CASE A +1000 vs truth −2450) — the harness is the B3 fix gate (assert see==ref_see).** Near-no-ops (do when convenient, ~0 Elo): delete the unreachable root killer update; pre-pass +1 TT depth inflation; `use_q_precautions` always-true cleanup.
>
> **🎯 (2026-06-11, LATEST) — PHASE-RESOLVED EVAL PROFILER: BUILT, VALIDATED, FINDINGS IN HAND.** Compile-gated `__rdtsc__` per-term profiler (`PROFILE_EVAL=1` → `-DEVAL_PROFILE`, fully `#ifdef`-walled so production is byte-identical — confirmed: clean production rebuild, no EVAL_PROFILE symbols, `PROF_BLOCK` → `((void)0)`). **5 files, UNCOMMITTED:** `setupAI.py` (env gate), `cpp_bitboard.h` (generic reusable `ProfTerm` enum + `g_prof_cycles/calls[]` + RAII `ProfScope` + `PROF_BLOCK(term)` macro; 9 API fns declared UNCONDITIONALLY so Cython always links, bodies `#ifdef`-walled), `cpp_bitboard.cpp` (21 top-level `PROF_BLOCK` sites + 5 nested drill scopes + accumulators/`eval_profile_reset/dump/run`/getters at EOF), `ChessAI.pyx` (`reset_profile`/`dump_profile`/`profile_eval(board,reps)`/`get_profile()`), `diagnostics/eval_profile.py` (8-phase corpus binning from `selfplay/games/<tag>/game_*/game.jsonl` + driver + per-term×phase %-share table). **Build via WSL `/home/ranuja/anaconda3/bin/python` (Windows python3 = PyPy); FORCE a clean rebuild when toggling PROFILE_EVAL — a flag-only change doesn't bump source mtimes on `/mnt`, so `touch cpp_bitboard.cpp cpp_bitboard.h ChessAI.pyx && rm -rf build ChessAI.cpp ChessAI.*.so` first.** Ran tag=newstack_uho (1475 games), reps=2000/pos, cap=200/bin. **KEY SIMPLIFICATION found in build:** `ev()`/`placement_and_piece_eval` never touches the eval cache (cache is only in `get_board_evaluation`) → no cache-bypass needed, just loop the eval M×. **Caveat:** ~50cyc rdtsc overhead per scope OVERstates cheap high-frequency terms (PAWNS @10.6 calls/ev) and understates expensive ones — so BISHOP_COLOUR's dominance is if anything understated. **NNUE-as-eval considered → BACKLOG** (full-refresh NNUE isn't faster than our eval; the speed win needs the incremental accumulator plumbing we've ruled out; accuracy win is real but a multi-day integration + margin re-tune). Memory: [[eval-profiler-plan]]. Plan: `~/.claude/plans/handoff-for-the-glistening-token.md`.
>
> **🎯 (2026-06-11) — IMPROVING HEURISTIC built + tested → SHELVED (gated, default-off); SEARCH lane is now closed; PIVOT TO EVAL.** **⚠️ NEXT: the eval is a DOUBLE bottleneck (imprecise = strength ceiling; AND slow = our non-incremental `placement_and_piece_eval` is too expensive to support per-node-eval search techniques). Next step = PROFILE the eval per-term across game phases (find the hotspots) before deciding precision vs efficiency work — and crucially we can keep ALL the rich hand-crafted features and just compute the expensive ones lazily / only-when-necessary, not rip them out.**
>
> **IMPROVING HEURISTIC verdict (`ENABLE_IMPROVING` + `IMPROVING_EVAL_WINDOW`, gated default-off, byte-identical; committed as a dormant knob):** reduce one extra LMR ply when the side-to-move's static eval isn't rising vs 2 ply back. Built with a node-entry **non-counting** static eval into `g_evalStack` (per-side sign flip: minimizer `<`, maximizer `>`) — NOT futility-reuse (early_score is the *child* eval). First build had the sign backwards (`+1` = reduce-less, +8.9% nodes); fixed to `-1`. Corrected d10 sweep (USE_OPENING_BOOK=0): **messy + non-monotonic, no clean win** — w4 +43STS/+3%n, w6 −69STS/−10.5%n, w8/w16 +72STS/+8-12%n (the STS-positive configs ADD nodes = re-search storms, not pruning). **Lightning 3-rep de-noise = NET LOSS: Δdepth −0.18 consistent** (improving cut nodes at fixed depth yet reaches LESS depth in play → the per-node eval cost outweighs the node savings), STS noisy ~neutral, WAC −4. The eval-cost-eats-the-gain risk the plan flagged, confirmed. **SHELVED.** Memory: [[improving-heuristic-shelved]].
>
> **THE PATTERN (two thorough shelves now — gravity, improving):** search-ordering/reduction is tapped out here (pruning-bound 92% FMC + eval-bound), and improving failed *specifically because the eval is too slow* to compute per-node. So **eval work pays off twice** — raises the strength ceiling AND unblocks the per-node-eval search lane. Three independent signals → **eval is the move.** First eval step = per-term profiling across phases (see `BASELINE_PERF.md`).
>
> **🎯 (2026-06-10) — GRAVITY/MALUS family EXHAUSTIVELY TESTED → SHELVED (gated, default-off); a bench bug was found + has a clean fix; the move-ordering lane is now closed.** **⚠️ NEXT-CHAT FIRST STEPS: (1) bench with `USE_OPENING_BOOK=0` from now on (the STS bench had a ±30 noise floor from flaky book-hit detection — see below); (2) re-baseline WAC/STS under it; (3) pick the next lever (eval precision is the strategic ceiling; improving-heuristic is the safe search grab).**
>
> **THE GRAVITY/MALUS VERDICT (uncommitted, all gated default-off):** built the saturating bonus/malus "gravity," then DECOMPOSED it into two orthogonal knobs — `ENABLE_HISTORY_SATURATION` (bounded `hist_update` vs simple `+=`) and `ENABLE_HISTORY_MALUS` (penalize searched-and-failed quiets/captures) — plus `MALUS_DIV` / `MAX_HISTORY` / `CONT2_GRAVITY_DIV` / `ENABLE_HISTORY_DECAY`. `iso_base` (both off) is **byte-identical** (262 / 267,284,369 / STS 1552 — the refactor is clean). Tested every way: d10 feature sweep (gravity+caphist best of a bad lot), decay×malus tuning (default = local optimum; "malus too hot" AND "decay redundant" both disconfirmed), **lightning 3-rep de-noise (gravity+caphist consistently WORSE in play, ΔSTS ≈ −34; the earlier single-run +17 was NOISE)**, and the orthogonal isolation matrix (`iso_sat` formula-alone **−73**, `iso_mal` malus-on-simple **−99** = WORST cell → the "keep proven scoring, add only the malus" hypothesis DISCONFIRMED; saturation actually *limits* malus damage since the unbounded simple scale lets the malus run unbounded-negative). **WHY it loses here:** the malus is an ordering-only signal with **no consumer** — we lack SF's history-driven pruning/LMP (where the malus actually pays off) — and we're pruning-bound (92% FMC) + eval-bound (depth doesn't convert). **PARKED, NOT DEAD: revisit only after building the history-driven pruning ecosystem and/or sharpening the eval. Original (proven bonus+decay) scoring stands as best.** Memory: [[ordering-history-design-ideas]].
>
> **THE BENCH BUG (important, reusable):** d10 STS had a **~±30 raw noise floor** — NOT engine nondeterminism (WAC node counts are byte-identical), but **flaky opening-book-hit detection**: `sts_test` greps captured stdout for `"Book Move"`, the fd-capture races, and ~3 book-hit positions flicker between excluded (blank) and scored (10 pts). So **STS deltas < ±30 across the whole project were noise**; WAC + de-noised lightning were always the trustworthy metrics. **FIX: bench with `USE_OPENING_BOOK=0`** (the engine already gates the book on `_USE_OPENING_BOOK` in `ChessAI.pyx`) → deterministic STS + pure-search on all 300; re-baseline owed. Memory: [[bench-determinism-book-flakiness]].
>
> **(superseded by the above)** 🎯 (2026-06-09/10) — eval-scale REVERTED (suite regression), 3 ordering features built (none yet a win), history GRAVITY/MALUS rework built (byte-identity UNCONFIRMED). **⚠️ NEXT-CHAT FIRST STEP: confirm gravity-off byte-identity.** My WSL-background run gave gravity-off WAC d10 = **258/300 vs the 262 control** but with **nodes=0 (a recording quirk of my piped WSL runs, not the engine)** — so it's SUSPECT, not confirmed-broken. Code review says the gravity-OFF path is the exact original `+= depth²` (I only hoisted `storeKillerMove`/`counterMoves`, independent tables). **Run INTERACTIVELY (records nodes): `MAX_DEPTH=10 python diagnostics/tactical_test.py wac.epd grav_off_check` → must be 262/300 AND 267,284,369 nodes.** If yes → byte-identical, run the gravity-ON benches below. If 258/different → real off-path bug, debug first.
>
> **COMMITS this stretch: `046a17f`** (shipped CH-A@2000 + endgame scale, both default-on), **`934513f`** (parallel `annotate.py` + `paired_analysis.py` + docs), **`ab070b7`** (REVERT endgame scale to default-off + add 3 gated ordering features). **Gravity rework is UNCOMMITTED** (search_engine.{h,cpp} + cache_management.h), pending the byte-identity confirm.
>
> **THE SCALE REVERT (the one banked win, `ab070b7`):** `ENABLE_ENDGAME_SCALE` shipped default-on in `046a17f` on 5 FEN spot-checks, but a scale-ON STS bench (never run before shipping) showed it changes far more leaf evals than the spot-checks implied: **−3.3 STS / −3 WAC vs scale-off for only −3% nodes = net suite regression.** Reverted to default-off (knob kept). **Default build is back to the validated `chA_2000` baseline: STS 51.7% (consistent-aggregation; "52.3%" earlier was a blank-denominator convention — same raw score), WAC 262, 267,284,369 nodes.** LESSON: bench eval changes on the scale-ON suites BEFORE shipping; never assume an "endgame" feature is suite-neutral (it fires at search leaves). Memory: [[ordering-history-design-ideas]].
>
> **THE 3 ORDERING FEATURES (`ab070b7`, all gated default-off — none a clean win at initial magnitudes, all share "too-hot → premature cutoffs → accuracy-for-nodes"):** `ENABLE_CONT_HIST_2PLY` (2-ply continuation via a per-ply move-stack `g_searchStack`, NOT param-threading; scoped out of qSearch) = LOSS at equal weight (WAC −3, +7% nodes — the move-2-back is noisier, swamps at full weight). `ENABLE_CAPTURE_HIST` (capture-history in the capture tiers) = marginal (+0.7 STS/+1 WAC, +2.6% nodes). `ENABLE_CHECK_ORDER` (flat `CHECK_ORDER_BONUS=6000` for direct-check quiets) = on the scale-off baseline −6 WAC for −9.8% nodes (too hot). **METHOD: scale-matched comparison is essential — check-order looked like +2.3 STS vs the scale-ON control but was only recovering scale damage.**
>
> **THE GRAVITY/MALUS REWORK (built, gated `ENABLE_HISTORY_GRAVITY` default-off):** replaces the crude bonus-only `+= depth²` cutoff update with a saturating `hist_update(h, ±b)` (`h += d − h·|d|/MAX_HISTORY`, `MAX_HISTORY=16384`) — **+bonus to the move that cut AND −bonus (malus) to the quiets/captures tried-and-failed first** (the new signal; needs a per-node `searched_quiets`/`searched_captures` list, appended after each child search). Applies to HH + 1-ply counter + cont2 (`b/CONT2_GRAVITY_DIV=b/4`, 2-ply down-weight baked in) + capture, in **minimizer (both loops) + maximizer** only (pre_minimizer + qSearch on the old path for v1). The hypothesis: a sharper foundation flips 2-ply/capture-hist net-positive. **GRAVITY-ON BENCHES (after byte-identity confirms):** `ENABLE_HISTORY_GRAVITY=1` alone, then `+ENABLE_CONT_HIST_2PLY=1`, then `+ENABLE_CAPTURE_HIST=1` — WAC+STS d10 vs the 262/51.7%/267.3M baseline; want fewer nodes / higher STS without WAC loss. Plan: `~/.claude/plans/sure-let-s-plan-it-compiled-rabbit.md`.
>
> **STRATEGY (user, standing):** accumulate cheap deterministic-bench-provable SEARCH wins (eval changes are NOT bench-provable → defer to tuning); overnight tournament only as a no-regression safety net. SPSA tunes MANY continuous params JOINTLY (noise-robust; not 1-vs-1 = that's SPRT) but needs a multi-night marathon (~10k games/week) — do it when compute aggregates. Deferred ordering ideas (memory [[ordering-history-design-ideas]]): front-promotion + epoch resort (cache staleness; low-value @92% FMC), staged/lazy move-gen (the nps win), qsearch capture-update gate, improving heuristic, learned check-history.

> **🎯 (2026-06-09) — SHIPPED continuation-aware LMR + endgame convertibility scale, both default-on (commit `046a17f`).** The overnight 8h `newstack`-vs-`base` UHO A/B ran **1475 games = +9.4 ±20.8 Elo — NOT significant** (the tournament's *own* paired margin; the naive hourly ±5-6 I reported were optimistic, exactly as flagged — shared-eval self-play can't resolve a correctness-sized change). No regression, balanced colours, 0 voids. **The benches decided, not the tournament.** **`ENABLE_CONT_HIST` (CH-A) default-on at `CONT_HIST_LMR_THRESH=2000`** — d10 threshold sweep: 8000 flat → 4000 (−1.6% nodes) → **2000 (−6.2% nodes, WAC 262/300, STS 52.3% neutral)** → 1000 rebounds to −2.7% (over-fires); 2000 is the bracketed node-efficiency optimum (continuation-favoured quiets, searched a ply deeper, cut off faster → net fewer nodes → deeper at fixed time). **`ENABLE_ENDGAME_SCALE` default-on** — FEN spot-checks clean: KQK/KRK/B+3P **byte-identical off→on** (edge>minor→s=1), OCB drawish **−0.64→−0.34 toward SF** (the `pattern_diag` aggregate barely moved BECAUSE the scale correctly leaves *winning* positions at s=1; the big `+1B/+1N` numbers are benign saturated wins). **`ENABLE_MATE_DRIVE_SCALE` stays default-off.** **NEW d10 CONTROL (replaces void 263,422,651): WAC 262/300, 267,284,369 nodes, STS 52.3%.** Also this session: `selfplay/paired_analysis.py` (imbalance-bucketed convert/defend on UHO pairs — **but convert-vs-defend is DEGENERATE in a head-to-head A/B**, need each engine vs a *common* reference) + **parallelized `annotate.py` (`--concurrency`, per-thread SF + lock-guarded ChessAI)**, both UNCOMMITTED. **ENV GOTCHA: Windows `python3` is PyPy → python-chess Stockfish comms break (ctype asyncio bug); run SF-dependent scripts via WSL `/home/ranuja/anaconda3/bin/python`.** **NEXT:** FULL 2-ply continuation history (thread `previousMove2` through the 21 minimizer/maximizer call sites — BoardState stores no move) + compact `[piece][to]` reindex of `counterMoveHeuristics` (134MB→~2.4MB); then improving heuristic → SPSA+SPRT loop. Plan: `~/.claude/plans/sure-let-s-plan-it-compiled-rabbit.md`.

> **(2026-06-08/09) — HARNESS THROUGHPUT UNLOCK shipped; 3 gated eval/search features written + overnight A/B (now resolved above).** **SHIPPED (commit `0e02285`):** self-play **game concurrency** (`--concurrency N`, ThreadPoolExecutor, per-thread SF arbiter, main-thread-only bookkeeping; N=1 = unchanged sequential) — measured **6.5×** on the 8c/16t box (8 games 17m21s→2m39s); **timed mode** (`--max-minutes`: play as many games as fit a wall-clock budget on one preset, greedy refill gated on measured per-game wall time, in-flight games drain); **`selfplay/gen_openings.py`** (UHO unbalanced-but-sound opening-book generator → `selfplay/openings_uho.txt`, 1000 lines, for diverse/decisive/independent self-play). `schedule()` already plays each opening from BOTH colors on consecutive games (convert+defend test). This is the throughput prerequisite for sound SPRT (a ~15-Elo effect needs ~1000 games). **THREE FEATURES WRITTEN, env-gated default-off = byte-identical, UNCOMMITTED (in cpp_bitboard.cpp + search_engine.{h,cpp}), pending bench:** (1) **`ENABLE_ENDGAME_SCALE`** — `endgame_convertibility_scale()` (before `placement_and_piece_eval`), a continuous [0.25,1] damp of an *unconvertible* endgame lead (bare-minor-up / opposite-bishops), pulled to 1 by an advanced winning passer; mates (edge>minor)→s=1; hooked at the boost block (scales `total` AND the `piece_value_boost` term). Bench = `pattern_diag --mode bias` (the `+1B/+1N` endgame end_mean should drop) + FEN spot-checks. (2) **`ENABLE_CONT_HIST` (CH-A)** — continuation-aware LMR: in `history_lmr_delta`, a tier-0 (never-cut) quiet with a strong 1-ply `counterMoveHeuristics` score is NOT reduced-more (cancel the −1, back to base; never deeper). ~8 lines, no param threading, `CONT_HIST_LMR_THRESH=8000` (tunable). Bench = STS300 + WAC d10 nodes (STS up + nodes-not-worse = on track). (3) **`ENABLE_MATE_DRIVE_SCALE`** — committed (`fb02b1b`), default-off. **TONIGHT (overnight, 8h):** combined A/B `newstack` (all 3 flags=1) vs `base` (all off) on the UHO book — `tournament.py --p1-config "ENABLE_MATE_DRIVE_SCALE=1 ENABLE_ENDGAME_SCALE=1 ENABLE_CONT_HIST=1" --p2-config "" --openings selfplay/openings_uho.txt --preset LIGHTNING --max-minutes 480 --concurrency 6 --adjudicate-draw --tag newstack_uho`. **⚠️ REQUIRES `python setupAI.py build_ext --inplace` FIRST** (the gated features must be compiled in; the current `.so` predates them). Morning: `annotate.py --tag newstack_uho` → `deep_diag.py` phase-split (mate→deep-mate, drawish→endgame-convert, CH→midgame ordering — a *lens*; the per-feature benches are the attribution of record). **NEXT SESSION:** read benches → commit the gated features that pass (no footer); then the FULL **2-ply continuation history** (thread a new `previousMove2` through the **21** minimizer/maximizer call sites — BoardState stores no move) + the compact `[piece][to]` reindex of `counterMoveHeuristics` (134MB→~2.4MB, de-thrash the 96MB L3). **Owed: all-off WAC d10 re-baseline** (the 263,422,651 control is void). Plan: `~/.claude/plans/sure-let-s-plan-it-compiled-rabbit.md`.

> **🎯 (2026-06-08, eval-precision arc cont.) — SHIPPED endgame draw-detection; reduce-more LMR now DEFAULT-ON; mate-drive scale now a DEFAULT-OFF knob.** Built `selfplay/pattern_diag.py` (miseval-pattern miner: material-config offender clusters / signed-bias-by-material-delta / transformation leaks; all trust-gated) + `term_diag.py` **trust gate** (NNUE-static is a valid yardstick ONLY where it tracks the SF SEARCH eval — drops NNUE-blind tactical positions where *we're* right). **Localized the #1 fixable eval bias = ENDGAME MATERIAL OVER-valuation:** up-a-minor reads **+461cp in the endgame vs +31 midgame** (bias_ratio ~1.0, stable across L/B/S); midgame material is ~calibrated ⇒ a phase-gated fix can't hurt gambits. The `piece_value_boost` amplifier (`diff/winnerTotalMaterial`) is CORRECT in direction (a fixed lead grows as you trade down) — the gap was unhandled *cliff* cases. **FIX (SHIPPED): extended `is_practically_drawn` (cpp_bitboard.cpp ~5029) with R+N-vs-R, KRKN, KRKB** (pawnless / exact-count guarded; returns 0 BEFORE the boost runs at ~5808). Validated: top offender cluster R+N-vs-R (−684) → **gone**, `+1N` endgame over-read **461→142**, FEN spot-checks (R+N-vs-R→0, *with a pawn*→still evaluated, KR-K/KBN-K untouched). **DEFAULTED: reduce-more LMR** (`ENABLE_HISTORY_LMR=1 CAP=0 MORE_CAP=1` — STS 50.1→51.7%, −1.3% nodes; self-play +25.5±65 not-sig but no-regression; knob stays for override). **NEW KNOB `ENABLE_MATE_DRIVE_SCALE` (default-off)** — the mate-drive material-margin scale, *preserved not shipped* (no definitive result; its R+N-vs-R target is now handled upstream by is_practically_drawn). **Behavioral ⇒ WAC d10 263,422,651 VOID — re-baseline owed.** Next eval follow-up = a **graded drawishness scale** (oppo-bishops / R-vs-2-minors / R+N-vs-R-*with*-pawn — the cases the binary detector can't express), which would also subsume the mate-drive knob. Plan: `~/.claude/plans/sure-let-s-plan-it-compiled-rabbit.md`.

> **(2026-06-08, earlier) — SHIPPED color-symmetry (ae8a462/b9c27bc) + history-LMR (b82ac09); now in the EVAL-PRECISION arc.** Memory: `eval-precision-term-attribution`, `history-lmr-reduce-more`, `eval-color-symmetry-fix`. **Committed:** color-symmetry (12 fixes, corpus mirror residual 0.000; new control **WAC d10 258/300 = 263,422,651 nodes**, STS300 50.1%); history-aware LMR mechanism (env-gated default-off). Self-play diagnostics tooling + SF adjudication also committed (030ea48/2459bf1/e5fef44). **Open decisions:** (1) **SHIP reduce-more LMR** — `ENABLE_HISTORY_LMR=1 CAP=0 MORE_CAP=1` validated (STS 50.1→51.7%, −1.3% nodes @d10; overnight match +25±65, converts/defends better, no regression) but **defaults still OFF pending the flip.** (2) The **mate-drive scale (below)** + **endgame pt_* instrumentation** are **UNCOMMITTED**. **Eval-precision findings (`deep_diag.py`/`term_diag.py`, new):** the #1 lever is **static-eval win%-divergence vs NNUE, worst in the endgame** (near-equal win%-scatter ~11% overall / **late-end 343cp = 24%**; pure static-vs-static, depth-independent). Driver = the **`pieces`/placement** term. Worst near-equal-endgame offenders = **queen-bearing SHARP/compensated positions** (NNUE ~0, ours ±1000-1460), amplified by **`advanced_endgame_eval`'s mating-drive**. **FIX v1 (UNCOMMITTED):** scale the mate-drive (cpp_bitboard.cpp 4336-4388) by the winner's **material margin** (`MATE_DRIVE_LO=1500`/`HI=5000` ramp; defender-queen→0); passer-shepherding (4406-4493) + `evaluate_kings_endgame` untouched. **d10 result: STS300 50.1% UNCHANGED, WAC 255/300 (−3 noise), nodes 259.6M (−1.5%)** = suite-neutral (expected; suites are midgame). **⚠️ NEXT-CHAT FIRST STEP: the mate-drive's TARGET validation is PENDING — re-run + read (a) the KX-K mating spot-check [`ai.ev_breakdown` snippet] and (b) `term_diag.py --tag hlmr_more1_lightning` to confirm endgame scatter dropped vs pre-fix 343cp/24%.** Honest caveat: the gate removes the AMPLIFICATION; the base compensation over-read is the fundamental NNUE-only gap (scatter drops, not to 0); v2 passer/king-exposure damp optional. Behavioral eval change ⇒ byte-identity 263,422,651 VOID ⇒ re-baseline owed if kept. New tools: `selfplay/{deep_diag,term_diag,sprt}.py`, `annotate.py --reeval`. Plan: `~/.claude/plans/let-s-do-both-update-fluttering-flask.md` Parts 5/5b/5c.

> **🎯 (2026-06-07) — EVAL COLOR-SYMMETRY FIXED (uncommitted, behavioral).** Memory `eval-color-symmetry-fix` + `tournament-color-bias-diagnostics`. Tournament re-analysis (2026-06-05/06) found the eval was NOT mirror-symmetric (startpos read **−0.5 for Black**; depth-dependent color tilt in self-play = mostly White's first-move advantage emerging with depth + this shallow tilt, NOT a deep bug). Built a **per-piece-type color-mirror probe** (`diagnostics/eval_symmetry.py` + 6 `pt_*` fields on `EvalBreakdown` + `br_pt_*` accumulators in the 6 midgame piece loops, read-only) and fixed **10 non-mirrored items in `cpp_bitboard.cpp`** (wrong attack-layer index `[0]↔[1]`, wrong sign `+↔−`, one-sided clamps `min(X,cap)→clamp(X,±cap)`, and one order-dependent king-file bonus). **Startpos color-mirror residual −1.008 → −0.008.** The eval is absolute Black-positive + a single constant root flip (`get_board_evaluation` ~4195), NOT negamax, so each color path must itself mirror. Full list in `eval-color-symmetry-fix` memory + `OPTIMIZATION_LOG.md`. **⚠️ byte-identity (254,973,405) is now VOID — MANDATORY: re-baseline WAC d10 nodes + STS300, refresh `BASELINE_PERF.md`, run a short old-vs-new self-play A/B (expect ~neutral) BEFORE building further.** Tiny residuals remain (startpos `pt_rooks` ~8mp; one corpus position `pt_bishops` ~0.28). Also shipped this arc: `selfplay/tournament.py --adjudicate-draw/win` (heuristic-gated single-SF-confirm adjudication), `selfplay/tournament_diag.py`, `selfplay/jsonl_to_pgn.py`, fixed-depth color sweep (`depth_color_sweep.sh`), `dev_notes/STRENGTH_BACKLOG.md`. **NEXT after re-baseline: history-scaled LMR** (reduce-less, keyed off the composite ordering score) — plan `~/.claude/plans/let-s-do-both-update-fluttering-flask.md` Part 3, roadmap memory `pre-nnue-strength-roadmap`. Deep-research meta-finding: the biggest missing lever is an **SPSA+SPRT tuning loop on the self-play harness** (constants are tuned, not guessed).

> **⚠️ DEPTH-LABEL CONVENTION CHANGED 2026-06-03.** `MAX_DEPTH` is now **literal** (`MAX_DEPTH=10` searches to depth 10). A historical `MAX_DEPTH=11` ≡ today's `MAX_DEPTH=10` ("d10"). The commands below are written in the literal convention; "d10"/"forced-depth-N" labels are unchanged.

**Read this first in a fresh session**, then the memory files (`engine-cpp-optimization`, `engine-opt-working-method`) and the per-item log (`dev_notes/OPTIMIZATION_LOG.md`).

> **✅ CORRECTNESS milestone (2026-06-04): SearchData desync crash family FIXED & VALIDATED** via the grouped-scores refactor (`SearchData={moves_list; vector<RootScore>}`; alpha_beta sole writer; PVS pop deleted). WAC nodes byte-identical (254,973,405), 259/300, speed neutral; the replay loop that logged `[INV]` 12/12 now logs 0. Full writeup: `dev_notes/CRASH_INVESTIGATION_PLAYBOOK.md`. The self-play harness that found it lives in `selfplay/` (memory `selfplay-harness`).

> **🔬 EVAL-CALIBRATION PROBE + NULL-MOVE RESULT (2026-06-05, LATEST).** `ev_breakdown` static-eval term-attribution probe built (instruments `placement_and_piece_eval` in-place via guarded `br_*` delta snapshots; exposed as `ChessAI.ev_breakdown(board)`; `diagnostics/eval_breakdown.py` prints three-way `our_static / SF_static / SF_search`; WAC d10 byte-identical 254,973,405). `Arbiter.evaluate_static` added (SF NNUE static via UCI `eval`). `selfplay/summary.py` + `overnight_battery.sh` added (STANDARD-weighted, timing-instrumented). Chess.com game saved as `selfplay/external/chesscom_2200_white.pgn`. **Eval finding:** across ~5k positions: `our_static − SF_static` mean = +0.03–0.09 overall (well-calibrated in balanced positions), but **winning-side split = +0.78–1.09p when White winning / −0.63 to −2.39p when Black winning** → systematic over-reward of the winning side, driven by the `pieces` (PST/placement) term. NOT `latent_threat` (0 in all endgames; correctly fires only with enemy in king zone), NOT `piece_value_boost` (tiny ~+0.05 mean). **King-attack-term thesis dead for conversion failures.** Root cause of the winning-side hot-read is PST evaluations over-valuing dominant/active placements in won positions. Fix requires **STS-guided PST tuning**, not a gate on a single term. **pvb env-gate night DEFERRED** (not the main driver). **NULLMOVE_PROGRESSIVE overnight A/B CONCLUDED:** at STANDARD (the only TC where it fires, genuine d≥12): `nmp_on` = +0.56 ply deeper but **+21% nodes / +15% time** — not free depth, search working harder not smarter (aggressive null-move generating fail-lows that must be re-searched). LIGHTNING inert (11.42 vs 11.52, gate works). W/L/D is noise (n=24 inert LIGHTNING arm swung 67% by variance → proved ±17pp noise at n<30). **VERDICT: NULLMOVE_PROGRESSIVE PARKED default-off.** Softer design (conditional reduction, not flat -1/-2 at fixed depth thresholds) needed before retrying.
>
> **🧭 STRATEGIC PIVOT (2026-06-04) — depth is EBF-bound, not nps-bound.** STANDARD self-play data: the engine averages **~d13 @ 21s/move** (~10M nodes, **effective branching factor ≈ 3.4**); 2010–2018 traditional engines reach ~d18–20 at EBF ~2.2–2.5. Since depth ∝ log(nodes)/log(EBF), **EBF dominates and raw nps barely matters** (a 10% PGO speedup ≈ +0.07 ply; EBF 3.4→2.5 ≈ +3.5 plies at the *same* node budget). ⟹ the path to ~3000 at the current system level (pre-NNUE/SMP) is **search efficiency (move ordering + pruning) + eval** — eval is coupled (better eval → stabler PV → more first-move cutoffs → lower EBF) — NOT raw-speed micro-opts. NNUE/SMP are the user's deliberate "easy multipliers", applied LAST.
>
> **EBF/move-ordering diagnostic — DONE, RESULT = PRUNING-BOUND.** Behavior-neutral `g_fh_total`/`g_fh_first` cutoff counters at the 3 main-search cutoff sites + a `[search]` stderr line (`first_move_cutoff% ebf=…`); byte-identical (WAC d10 still 254,973,405). **Measured `first_move_cutoff = 91.9%`** (strong-engine range 90–95%) → **move ordering is already good; the EBF gap (3.4–3.65 vs ~2.2) is PRUNING-bound.** ⟹ **do NOT invest in move ordering**; the depth lever is **more-aggressive pruning (LMR/null-move/futility) + a smarter verification/re-search safety net** (the engine prunes conservatively for tactical safety — `VERIFY_MARGIN` exists for exactly that, so "prune harder + verify smarter" without losing the deep tactics; validate on **STS + self-play**, not node/depth alone). The **null-move depth-adaptive overnight (queued) is step 1** of this pruning work.
>
> **Speed micro-opt sequence (2026-06-04):** item 1 `-fno-semantic-interposition` KEPT (+~2%, commit `5acfb7f`); `-fno-wrapv` reverted (neutral); dead-`use_tt_entry1`/TT-field-trim skipped (DRAM-bound TT; 24B straddles cache lines); move-scratch-buffers skipped (~0.3%). **Null-move depth-adaptive** `NULLMOVE_PROGRESSIVE` (env-gated default-off) = depth-dependent (−18% nodes @ genuine d12 / +4.7% @ d10 via check-ext inflation); refined to gate the extra reduction on `depth_limit - g_check_extensions ≥ 12/14`; **overnight A/B parked → `dev_notes/NULLMOVE_OVERNIGHT.md`**. **PGO deferred** to the finisher (profiles final code; cheap to redo). Clean base = `d20fb6b` (checkpoint) + `d839c4e` (crash fix); **NOT pushed** (public repo, awaiting explicit OK). Repo hygiene: added `NN Engine/.gitignore` + untracked machine-specific binaries/outputs.

## What we're doing
Optimizing the **C++ chess engine** in `NN Engine/` (entry `ChessAI.pyx` → `search_engine.cpp` + `cpp_bitboard.cpp` (eval) + `cache_management.h` + `move_gen.h`). It's a **non-standard alternating minimax** (separate `minimizer`/`maximizer`, NOT negamax), **absolute eval** (positive = Black good, flipped once via `Config::side_to_play`), ~2700 Elo, **piece values pawn=1000 … queen=10000** (so its "centipawn" ≈ our/10). Build/run in **WSL** from `NN Engine/`: `python setupAI.py build_ext --inplace`. The user compiles & runs; you read code, design, and diff their output.

## Working method (do not skip)
- **One change at a time, measured**; rebuild → run → keep/revert before the next. Verify every AI/agent claim against the actual code (they overstate).
- **Speed items: gate = byte-identical** (eval/move/PV/nodes) + faster, else revert. **Measure at a FORCED FIXED DEPTH** — the engine straddles a depth-step boundary, so time-bounded totals swing a whole ply and aren't comparable across builds.
- **Strength items: behavioral** (output changes) — judged on the **tactical harness**, NOT byte-identity, at a fixed depth. Watch for **churn**: a *correct* fix that regresses positions reveals the order-sensitivity.

## SPEED TRACK — DONE & kept (all byte-identical)
Bug1/Bug2 (cache fixes), Item 1 (move-sort memoization), **Item 3a** (TT→direct-mapped `std::vector<TTEntry>` array), **E1** (PEXT sliding-attack tables: `SlidingRow` w/ `operator[](occ)=data[_pext_u64(occ,mask)]`; externs in BOTH cpp_bitboard.h *and* move_gen.h), **E2a** (`pawn_rank_bonuses` map→`std::array<int,64>`), **E2b** (`CaptureInfo` vectors→fixed `CaptureStack`). **Net: ~16.7 s → ~9.2 s at fixed depth 11, ~190k→~313k evals/s.** Closed/do-not-revisit: move-ordering/hash-move avenue (Items 2, 3b failed). Parked lower-value speed: E3 (incremental eval state), per-ply move-buffer refactor, F-catalog micros — see plan.

## STRENGTH TRACK — major arc SHIPPED (default-on, validated in real play). SEE-gating tried → **parked (negative)**. STS positional harness built + `sts300_d10` baseline LOCKED = **1387/2970 (46.7%)** (weak abstract: AKPC 28%/a-b-c-advance 30%/King-Act 38%/Open-Files 40%; strong concrete: B-v-N 64%/Recapture 60%/Simplify 58%). Current: **aspiration windows + honest-root-TT VALIDATED keeper (env-gated, default-OFF pending DELTA sweep).** Prior attempt corrupted because 4 root/preliminary TT stores hardcoded `TTFlag::EXACT` (sound only under the infinite window; a narrowed window mislabels PV bounds as EXACT → poisons the re-search). Fix = honest flag at the 4 sites (`HONEST_ROOT_TT`) + incremental-widening aspiration (`ASPIRATION_*`). Validated: STS@d10 **46.7→48.9% (+2.2pp)**; LIGHTNING equal-clock **+4 solves / +0.32 depth (median 9→10)**, time-neutral; **−18% nodes/−22% time** @d10; broken-vs-fixed A/B = **STS +45 while WAC blind** (the proof the third rung is necessary). Next = **sweep `ASPIRATION_DELTA`** (deterministic d10 nodes-knee → LIGHTNING+STS confirm) to lock the shipped value, then flip DELTA + `HONEST_ROOT_TT=1` on as defaults.

**Diagnostic tools — now under `NN Engine/diagnostics/`** (moved 2026-06-01 to de-clutter). Each has a header block (`sys.path.insert(ENGINE_DIR)` so `from ChessAI import ChessAI` works — the built `.so` stays in `NN Engine/`; `SUITES_DIR`/`RESULTS_DIR`; bare suite names resolve against `suites/`). **Run from `NN Engine/`:** `python diagnostics/tactical_test.py wac.epd <tag>`.
- `diagnostics/tactical_test.py <epd> <tag>` — runs the engine over an EPD suite → `diagnostics/results/tactical_results_<tag>.csv` + `tactical_fails_<tag>.epd`. Book hits auto-flagged.
- `diagnostics/sts_test.py <epd> <tag>` — **POSITIONAL** suite (Strategic Test Suite). Scores move-CHOICE via the EPD's `c9` (UCI moves) + `c8` (parallel points) ops → `{uci: score}`; reports total %/max + per-theme. Reuses `tactical_test.run_one`. Suite: `suites/STS1-STS15_LAN_v3.epd` (1500 pos, get via `wget https://raw.githubusercontent.com/fsmosca/STS-Rating/master/STS1-STS15_LAN_v3.epd`); fast sampler `suites/sts300.epd` = `awk 'NR%5==1'` of it (300, 20/theme). Run fixed-depth for a *deterministic* reference.
- `diagnostics/suites/` — `wac.epd` (WAC 300, tactical), `STS1-STS15_LAN_v3.epd` + `sts300.epd` (positional), `tactical_fails_deep12.epd` (the 43 fundamental fails), `resolution_probes.epd` (the 4 cluster probes).
- `diagnostics/ablation_sweep.py` — subprocess-per-combo env sweeps → position×combo matrix.
- `diagnostics/eval_at_resolution.py` (+ frozen `results/eval_resolution_set.json`), `diagnostics/line_probe.py`, `diagnostics/stockfish_oracle.py <epd> <depth>` (SF 16.1 at `…/Chess Engine/stockfish/stockfish-windows-x86-64-avx2.exe`).

**Env knobs** (read once/proc in `initialize_engine`, echoed to stderr `[toggles]`). **Defaults now = the shipped strength config, NOT the old byte-identical build** — to reproduce the 240/300 @d10 control set `VERIFY_MARGIN=0 CHECK_EXTENSION=0 REPETITION_THRESHOLD=3 ASPIRATION_DELTA=0 HONEST_ROOT_TT=0 MAX_DEPTH=10`. Knobs: `ENABLE_LMR/FUTILITY/RAZORING/NULLMOVE/QDELTA` (default 1), `PROTECT_KILLERS/PROTECT_PV` (0), `VERIFY_MARGIN` (**6000**)/`VERIFY_RESEARCH_REDUCTION` (**2**), `REPETITION_THRESHOLD` (**2**), `CHECK_EXTENSION` (**3**, per-path cap), `SEE_EXTEND_MARGIN` (disabled=1000000; lower to filter spite checks), `MAX_DEPTH` (**64**; =10 pins the d10 control), `PRESET` (LIGHTNING/BLITZ/STANDARD/LONG_FORMAT → `Config::ACTIVE`), `LMR_PROFILE` (0)+`PROFILE_BM`, **`HONEST_ROOT_TT`** (**default 1 now**; 0=hardcoded EXACT=the byte-identical control), **`ASPIRATION_DELTA`** (**default 500 now**; 0=off=full window), **`ASPIRATION_MIN_DEPTH`** (5), **`ASPIRATION_MAX_WIDENINGS`** (3), **`ASPIRATION_WIDEN_PCT`** (200=×2/resize). `ChessAI.ev(board)` = raw static eval (Black-positive; White-cp = `-ev/10`).

### What we PROVED (the chain)
1. **Eval is SOUND, not the bug.** Built `ChessAI.ev()` + `eval_at_resolution.py` (freeze SF deep-PV resolution FENs once, re-score our *static* eval there). Wherever a static-vs-static comparison is meaningful (WAC.204 ≈ equal, .018 White +10) our static **agrees with Stockfish**. The deep-misevals (engine thinks the winning side is losing, e.g. WAC.213 search ≈ −9 pawns) are **forcing/mating-sacrifice lines** — a static eval can't represent a mate; that −9000 is the SEARCH-backed value. ⇒ **the bug is the search.**
2. **LMR is the SOLE culprit** (ablation via `ENABLE_*` toggles + `ablation_sweep.py`, deep12 @ d10): baseline 3/43; all-pruning-off 32/43; **`no_LMR` alone 31/43 (+28)**; futility/razoring/null-move/q-delta each +0…+3. Mechanism: an LMR-reduced quiet move fails low at reduced depth; the re-search gate (`score>alpha && score<beta`) never fires on the fail-low → the move dies at its reduced value (no extensions exist).
3. **Stockfish ladder** (deep12): SF d10 18 / d12 23 / d15 28 — SF *also* over-prunes at a hard d10 (it needs depth-margin), so "solve @ fixed d10" is a confounded metric; but SF's d10 (18) >> ours (3) ⇒ its cuts are smarter. Real strength = depth-per-second. ~8 of the 43 are genuinely **depth-gated** even full-width (incl. WAC.213).
4. **Profiler killed the easy levers.** `LMR_PROFILE` bucketed 5.8M reductions: dropped moves are uniformly **low history-VALUE** (so "reduce-less-for-high-history" can't fire) and indistinguishable from noise by every local signal ⇒ selection-heuristic LMR is tapped out; **verification (re-search) is the lever.** Dead ends, all measured: `PROTECT_KILLERS` +0 (killers already ordering-boosted), `PROTECT_PV` +7 but **4.3×** (full-window nodes ≈ half the tree; PV move already protected via the live PV ordering bonus), `DELTA_MARGIN` (q-delta-off = ∞ margin = +0).

### SHIPPED — the strength arc (all default-on; full per-item numbers in `OPTIMIZATION_LOG.md`)
1. **VERIFY re-search** (default VM=6000, RR=2): re-search a reduced move that fails low within VERIFY_MARGIN of alpha, at `depth_limit−RR`; both `get_score_*` gates. WAC@d10 240→249 (r2)/257 (r1) — shipped **r2** (real-play-better; r1 won bigger at d10 but its 1.76× cost plies in time-limited play).
2. **REPETITION_THRESHOLD 3→2**: score the *first* in-search repetition as a draw → fixes won-games-walking-into-perpetual-draws (the h8=Q "+14.5" that's really a forced-draw perpetual; flat eval d10–22 = a *detection* gap, not depth). WAC@d10 +6 (255).
3. **Timeout best-move fix** (`get_engine_move`): adopt an iteration's result only if `alpha_beta` returned without `time_up`. Fixed a real lost game — d12 found Rg8, d13 timed out mid-`reorder_legal_moves`, and the abort played the refuted h8=Q via the stale `moves_list[0]` fallback. Position-general (any iteration that changes its best move, then the next times out).
4. **CHECK_EXTENSION** (default cap 3): a checking move searches +1 ply, per-path-capped (file-scope counter + RAII guard); default-off is byte-identical. Equal-clock real play: **+9 @d10, +20 @LIGHTNING, +7 @BLITZ — free on the clock** (early mate cutoffs cut total time). All 13 d10 gains are depth-gated forcing wins (each solves at d16). Fixes the perpetual *reliably* (no d12 coin-flip).

## NEXT STEPS (as of 2026-06-05)
1. **COMMIT the uncommitted session work** (all on `NN-ENgine`, no-rebuild, default-ON = byte-identical):
   - `ev_breakdown` probe: `cpp_bitboard.{h,cpp}` (struct + globals + term-capture in `placement_and_piece_eval` + `eval_breakdown_capture` wrapper) + `ChessAI.pyx` (`ev_breakdown` method) — **commit first, labeled "eval-breakdown probe"**.
   - `arbiter.py` (`evaluate_static`) + `diagnostics/eval_breakdown.py` + `selfplay/summary.py` + `selfplay/overnight_battery.sh` — **commit as "harness: eval-calibration telemetry + overnight battery + summary tool"**.
   - `annotate.py` telemetry updates + `selfplay/external/chesscom_2200_white.pgn` — **commit as "annotate: sf_static + ev_breakdown telemetry + chess.com corpus entry"**.
   - Push when user OKs (public repo).
2. **PST tuning via STS** — the eval-calibration data localized the winning-side over-reward to the `pieces` (PST/placement) term. STS300 baseline = 48.9% (post-aspiration). To improve: trial STS-guided adjustments to individual piece placement tables (start with the AKPC/King-Activity/Open-Files themes = the weakest). Validate each on STS300 (gate) + self-play (judge) + WAC (over-correction guard). Do NOT gate pvb/advanced-endgame — they're not the driver.
3. **Draw/resign adjudication in `selfplay.py`** — both engines same code → any draw bias cancels → safe to implement. After min plies (~40), if |eval| ≤ ~0.5p for N consecutive plies → call draw; if |eval| ≥ ~8–10p for N consecutive plies → call win. Env-knobs for thresholds. Cuts the long-tail standard games (the 1.5–2h 50-move R-v-R grinds) and would reclaim ~20–30% of the overnight budget.
4. **Softer null-move design** — `NULLMOVE_PROGRESSIVE` was too aggressive (flat -1/-2 at fixed depth → generates fail-lows → re-searches → +21% nodes). Candidate: condition the extra reduction on the evaluation being clearly ahead (not just depth), or use a smaller single step (-1 extra only at d≥14 not at d≥12). Validate at STANDARD (the only TC where it fires).
5. **LMR / futility tuning** (the main EBF lever) — the diagnostic says pruning-bound; VERIFY_MARGIN fixed the most obvious symptom; the next pass is softening LMR's reduction magnitude at high depths without blowing up nodes. Watch `[search] ebf=` as the target metric.
6. **SEE filter on extension — PARKED** (`SEE_EXTEND_MARGIN` knob kept, default disabled; net loss when tried).
7. **PGO = finisher** (profiles final code); **NNUE/SMP = last** (user's deliberate "easy multipliers").

## Cache architecture (keep in mind)
ALL caches (TT `searchEvalCache`, `evalCacheNew`, `quiesceEvalCache`, `moveGenCache`, history/moveFrequency/killers/counters) are **file-scope globals, shared across `ChessAI` instances, never cleared** (`update_cache` only prints; eviction commented out). Real-play = speed benefit (warm, bounded) — do NOT clear per move. Diagnostics needing isolation MUST fork. Backlog tooling (not gameplay): a `clear_caches()` Cython hook.

## Pointers
- Memory: `engine-cpp-optimization` (state/roadmap/decisions), `engine-opt-working-method` (disciplines + lessons). **Read both first.**
- `dev_notes/OPTIMIZATION_LOG.md` — per-item results incl. the STRENGTH TRACK table. `dev_notes/DEPTH_AND_POSITIONAL_NOTES.md` — parked strategy (why SF reaches d30; positional/NNUE direction). `dev_notes/NULLMOVE_OVERNIGHT.md` — null-move A/B design (concluded, result = parked).
- **Eval diagnostics** live in `diagnostics/eval_breakdown.py` (three-way our/SF-static/SF-search + term breakdown) and `selfplay/summary.py` (per-arm telemetry + calibration across a tournament tag).
- The default build = the shipped strength stack (**259/300 WAC @d10** with aspiration + VERIFY + check-extension). To reproduce old byte-identical baseline: `VERIFY_MARGIN=0 CHECK_EXTENSION=0 REPETITION_THRESHOLD=3 ASPIRATION_DELTA=0 HONEST_ROOT_TT=0 MAX_DEPTH=10`.
- The user compiles & runs in WSL; you read code, design, diff their pasted output. Distrust confident claims — verify against code.
