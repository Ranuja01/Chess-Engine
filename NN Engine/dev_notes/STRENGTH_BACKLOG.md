# Strength Backlog — search / eval improvement candidates

Living tracking table for the pre-NNUE strength track. Source-of-truth for *queued / in-flight* work;
**shipped** items graduate to `OPTIMIZATION_LOG.md` (STRENGTH TRACK), strategic context lives in
`DEPTH_AND_POSITIONAL_NOTES.md`. Derived from the deep-research roadmap (memory
`pre-nnue-strength-roadmap`) + the tournament/eval diagnostics (memory `tournament-color-bias-diagnostics`).
Last updated: **2026-06-14**.

> **▶️ STATE (2026-06-15, EBF sprint):** **cutoff histogram DONE → engine is PRUNING-bound** (96% of cutoffs at move 0/1, m8+ 1.3%; committed `be5a1b1`). **LMP BUILT + committed gated-off** (`8eaf80e`; `ENABLE_LMP`, reuses `do_lmr` eligibility, byte-id 262/260,960,881) — powerful but **over-prunes at fixed depth** (aggressive B3/MAXD3 −47% nodes/WAC 255/STS 51.5%; conservative B12/MAXD3 −25%/259/50.2%; **non-monotonic → self-play is the arbiter**, needs an overnight race). **▶️ ORDERING-FOR-PRUNING-SAFETY RESOLVED (lazy-hybrid LOCKED, tournament-pending):** gravity/malus **DROPPED** (re-tested WITH the LMP consumer it was "waiting for" — still STS-net-negative at every `MALUS_DIV`; root cause is **global-history pollution**, not a missing consumer); **lazy-hybrid cached-quiet re-sort, GENTLE config (no full resort, K=2, idx>2) = WAC 261 / STS 52.2% (held) / 134.4M nodes (−48.5%)** — baseline strength on BOTH benches at half the nodes (the aggressive full-resort variant cratered STS to 47.0% = malus's failure mode). Timed-depth GREEN LIGHT (**+0.53/+0.57 ply at equal time, +3 WAC / +2.3% STS**). **▶️ SHIPPED default-on (2026-06-16): tournament +59.2 ±19.2 Elo (1729 games, LIGHTNING); +0.50 ply at equal time. NEW DEFAULT BASELINE WAC 261 / 134,429,469 / STS 52.2% / EBF 4.20** (env-off `ENABLE_LMP=0 ENABLE_LAZY_RESORT=0` recovers 262/260,960,881 byte-exact). Analytics: fast better at every imbalance bucket + wins from behind; losses are ~70% collapse-from-okayish + ~25% squandered-winning in LONG games (NOT bad starts) ⇒ **eval/horizon precision is the lever**. **▶️ COLLAPSE DIAGNOSTIC DONE (`flip_extract.py`+`fen_vs_sf.py`): ~90% of losses = MIDGAME eval OVER-OPTIMISM** (we over-read vs SF and the error GROWS with depth — calibrated evals don't; deeper search steers into positions the biased eval over-rewards → fantasy advantage → collapse); passer/endgame ~10% = horizon. **EVAL PRECISION is THE ceiling, not search.** **▶️ NEXT = (B) passer TRUE-passed-mask fix (concrete-ready; the ADV=5 rank-proxy over-fired 825k/+12% nodes/STS−3.8%) → (C) FEN-based eval-tuning system: Texel inner loop (SF-distill + control set) + SPRT outer gate ("FEN-tuning for speed, SPRT for truth"); user's agentic-orchestrator vision = likely a Claude skill** → then SEE-pruning. Memory [[collapse-eval-overoptimism]]. Plan `~/.claude/plans/all-docs-current-and-woolly-phoenix.md`, memory [[lazy-hybrid-lmp-ordering-safety]], [[ebf-lmp-ordering-sequence]]. — *(prior:)* **eval-SPEED bundle SHIPPED default-on** (`535513f`): rook mobility surrogate + lossless attack-layer caches (endgame + split-half midgame) → **+8.6% nps / −3.8% wall, self-play +11.2 ±20.7 Elo (1487 games)**. New control baseline **WAC 262/260,960,881, STS 52.3%** (old 260/249,966,786 recoverable via the three `ENABLE_*` knobs `=0`). **Cheap-mobility does NOT generalize past the rook** — queen (STS −66; the don't-count-enemy-defended-squares filter is load-bearing for the queen) + knight (+11% node bloat; term already cheap) built/benched/REJECTED, retained gated default-off. The eval-CORRECTNESS lane was already exhausted; **the eval-SPEED lane is now also largely tapped** (surrogates don't generalize; remaining class = lossless caches). **▶️ PRIORITY = the EBF sprint** (EBF ~4.6 @d10 vs CPW-optimal ~2; cheaper eval makes eval-cost-gated pruning affordable — the speculatively-prune→verify pattern). **LMP + SEE-pruning are both confirmed ABSENT in the main search** (the rows below) → the high-leverage track; do the **cutoff move-index histogram diagnostic FIRST** (reuse `g_fh_total`/`g_fh_first` at the beta-cutoff points → decides pruning vs secondary-ordering emphasis). **Improving heuristic = SHELVED but QUEUED for re-test** (lost −0.18 ply when eval costlier, now ~8% cheaper). Plan: `~/.claude/plans/all-docs-current-and-woolly-phoenix.md`. Memory: [[eval-speed-bundle-shipped]].

Status tags: `idea` → `discussed` → `designed` → `testing` → `shipped` / `parked`.
Discipline: env-toggle default-off (off = byte-identical) → STS300 gate + WAC over-correction guard →
overnight adjudicated self-play tournament as judge. Constants are SHAPES — SPSA-tune, don't hand-pick.

| Item | Reuses | Status | Risk | Notes |
| --- | --- | --- | --- | --- |
| **Color-symmetry fix** | `placement_and_piece_eval`, `ev_breakdown` probe, `eval_symmetry.py` | **shipped** (ae8a462/b9c27bc) | med | 12 non-mirrored items fixed; corpus mirror residual 0.000; WAC d10 re-baselined **263,422,651**, STS300 50.1%. |
| **History-scaled LMR (reduce-less)** | composite ordering score | **dropped** (harmful) | — | Standalone reduce-less (CAP≥1) cost ~+10% nodes, STS flat-to-down; the reduce-MORE arm shipped instead. Revisit only with continuation history. |
| **History-scaled LMR (reduce-more)** | `reduced_search_depth`, `lmr_hist_tier` | **shipped** (default-on 2026-06-08) | med | `ENABLE_HISTORY_LMR=1 CAP=0 MORE_CAP=1` — reduce never-cut (tier-0) quiets 1 ply more. STS300 50.1→51.7%, −1.3% nodes @d10; self-play +25.5±65 (not sig, no regression). Knob retained for override. |
| **Continuation history** | `counterMoveHeuristics` (1-ply), `history_lmr_delta` | **CH-A shipped** (default-on `046a17f`) | med | **CH-A:** continuation-aware LMR — a tier-0 quiet with a strong 1-ply `counterMoveHeuristics` score is NOT reduced-more (cancel −1, back to base). `CONT_HIST_LMR_THRESH=2000` (d10 sweep optimum: **−6.2% nodes**, WAC 262, STS neutral; 1000 over-fires, rebounds to −2.7%). **Next (deferred):** full 2-ply table (thread `previousMove2` through 21 minimizer/maximizer sites — BoardState stores no move) + compact `[piece][to]` reindex (134MB→~2.4MB, de-thrash L3). |
| **SEE pruning (main search)** | `see()` (cpp_bitboard.h) | **NEXT (after the LMP/lazy-hybrid tournament)** | med | CONFIRMED ABSENT in main search (`see()` only used for ordering + qsearch filter today). Skip quiets with SEE < −margin·depth (quadratic) + losing captures (linear margin). Prior `SEE_EXTEND_MARGIN` attempt cut a sac-mate → guard hard on WAC. Now affordable post-eval-speed; the next absent EBF lever (after passer + the eval-precision work). |
| **Passer true-passed-mask exemption** | `passed_span_white/black[64]`, `do_lmr` fold, `getPPIncrement` | **concrete-ready (~10% horizon patch)** | low | ADV=5 rank-proxy `ENABLE_PASSER_PRUNE_EXEMPT` OVER-FIRED (825k/+12% nodes/STS−3.8% — exempted ALL advanced pushes). Fix: precompute the 3-file forward span (mirror `king_ring2`) + require true-passed `(enemy_pawns & passed_span[mover][to])==0`. Test FEN-probe-FIRST at LIGHTNING, then bench. Plan + [[collapse-eval-overoptimism]]. |
| **FEN-based eval-tuning system (~90% lever)** | `eval_fen.py`, `arbiter`/`annotate`, `flip_extract.py`, `sprt.py`; eval weights | **design (PAUSED; likely a Claude skill)** | high | Tame midgame eval OVER-OPTIMISM (the collapse root). **Texel inner loop** (corpus = over-read FENs + control set, labeled SF-eval/game-result; minimize sigmoid-error over exposed eval weights; cold-eval workers, seconds-not-games) + **SPRT outer gate** (no-regression Elo). User vision = agentic orchestrator queuing 100s of FENs; agent judges direction/regression/new-weaknesses with math feedback. [[collapse-eval-overoptimism]]. |
| **Late move pruning (LMP)** | `do_lmr` eligibility in `get_score_for_{min,max}imizer` | **SHIPPED default-on (2026-06-16, +59.2 Elo)** | med | `ENABLE_LMP`/`LMP_MAX_DEPTH`/`LMP_BASE`/`LMP_SCALE`; prune late quiets at low rd (threshold `BASE+SCALE·rd²`), reuses `do_lmr` (never prunes forcing). Over-prunes alone (−7 WAC @B3) — the lost solves are history-invisible positional quiets — but the **gentle lazy-hybrid recovers them**: B3 + lazy gentle = WAC 261 / STS 52.2% / −48.5% nodes. **B3 is the operating point** (B2 push-harder worse). Overnight tournament is the Elo arbiter. |
| **Cutoff move-index histogram** | `g_cutoff_histogram` at beta-cutoffs | **DONE** (`be5a1b1`) | none | 96% cutoffs at move 0/1, m8+ 1.3% → **PRUNING-bound, not ordering-bound**. Dispatcher `wac` prints `HIST:`. Re-run after changes to track EBF shift. |
| **Ordering-for-pruning-safety** | gravity/malus knobs + lazy-hybrid | **RESOLVED** | med | P1 gravity/malus × LMP → **malus DROPPED** (re-tested with the LMP consumer, still STS-net-negative; root cause = global-history pollution, not a missing consumer — falsifies the old shelve theory). P2 lazy-hybrid → **WON (gentle)**. The fallback risk (history-invisible quiet tactics) was real for the *aggressive* variant (STS 47.0) but the gentle nudge avoids it. Winner = the lazy-hybrid row below. |
| **Lazy-hybrid cached-quiet re-sort** | `buildMoveListFromReordered`, `MoveEntry`, `score_quiet` (move_gen.h) | **SHIPPED default-on (2026-06-16, +59.2 Elo; with LMP)** | med | On cache hit, re-rank the stale quiet tail by LIVE `score_quiet`, pinning captures+killers/counter. `MoveEntry`+=`last_cutoff_index`/`reuse_count`; cutoff index recorded in `updateMoveCacheForBetaCutoff`. **🔒 WINNER = GENTLE: `PROMOTE_TOP_K=2`, `LAZY_RESORT_MIN_CUTOFF_IDX=2`, full re-sort OFF (`RESORT_AFTER_REUSES` high)** → WAC 261 / STS 52.2% / 134.4M nodes (−48.5%) with LMP B3. **The periodic full re-sort was the STS killer** (aggressive K=4/idx>1/full = STS 47.0 — over-concentrates survival on history); "nudge, don't reorder." Timed-depth +0.53/+0.57 ply at equal time. |
| **Futility-margin tuning** | `FUTILITY_MARGINS` (constexpr → knobs) | **queued (after LMP)** | low | Already active (margins {200,450,650,950}@rd1-4, tight). Promote to runtime knobs, tune vs the new pruned tree — **SPSA, not 1-by-1** (continuous, overfits to bench noise). |
| **Decay-knob unification + tune** | `decayHistory/CounterMove/ContHist2/CaptureHistory` (`DECAY_FACTOR`), `decayMoveFrequency`, `increment_node_count_with_decay` (`DECAY_INTERVAL`) | **idea (SPSA-cluster, after pruning)** | low | The decay is hand-set + inconsistent: history family `>>=DECAY_FACTOR`(=1, ÷2) every `DECAY_INTERVAL` nodes, counter/cont2 every `×16`, moveFrequency `>>=2`(÷4) on a *depth* schedule (d13/%5). `DECAY_INTERVAL` is preset-set (125k–200k), NOT env-tunable. **Prep = unify + expose all as env knobs (byte-identical at current values), then SPSA** — decay is node-budget/game-length dependent, so it's a **self-play target, not a fixed-depth sweep**. Same signal-quality lever the malus result showed matters. |
| **Timed-depth bench (tooling)** | `wac_timed_depth`/`sts_timed_depth` (overnight_runner.sh), `tactical_test.py` CSV | **BUILT** | — | Lightning, uncapped-depth bench that reports solves + **mean depth excl mate-found** (mate stops deepening early) + mate count — the confound-free "did we go deeper, honestly" read that sidesteps the d10 ceiling. Fixed a latent `tactical_test.py` flush bug (eval/nodes CSV columns were blank; mate sentinel 9999995). See [[fixed-depth-bench-ceiling]]. |
| **Improving heuristic** | per-node static eval (+ small per-ply eval stack) | discussed | low | `staticEval > staticEval[2-ply]` → modulate LMR/futility (prune less when not improving... safe direction). Cheap; feeds the LMR work. |
| **SEE-ignores-pins fix** | SEE / capture eval (`move_gen.h`), `get_relevant_pin` | idea | med | From game review (`jun6_standard/game_006` move-55 double-blunder): a pinned-but-winnable piece reads as defended → easy 2-ply material wins missed. Localize with `line_probe`. |
| **Incremental eval** | board make/unmake, eval accumulator | parked (E3) | high | Update PST/material incrementally + pawn-hash; debug recompute-assert (WAC harness). Biggest nps lever. Major refactor — defer. |
| **Lazy eval** | eval term structure, in-window bounds | parked | med | Early-exit with material+margin before expensive terms. Pairs with incremental eval. |
| **Staged / partial move generation** | `generateLegalMovesReordered` cache | discussed | med | Generate captures→killers→quiets→bad-captures lazily; stop on cutoff. Promote-to-front already gives ordering win → this is an *nps* win. |
| **Endgame dead-draw detection** | `is_practically_drawn` | **shipped** (default-on 2026-06-08) | low | Extended with R+N-vs-R / KRKN / KRKB (pawnless, exact-count, returns 0 before `piece_value_boost` runs). Found via `pattern_diag` as the #1 endgame over-read (R+N-vs-R −684→0; `+1N` endgame bias 461→142). Leaves the boost's trade-down logic intact everywhere it's a real win. |
| **Graded drawishness scale (endgame scale factor)** | `is_practically_drawn`, material/pawn signals, `is_white_square` | **shipped** (default-on `046a17f`) | med | `endgame_convertibility_scale()`: continuous [0.25,1] damp of an *unconvertible* endgame lead — bare-minor-up / opposite-bishops, pulled to 1 by an advanced winning passer; mates (edge>minor)→s=1. Scales `total` + the `piece_value_boost` term at the boost block. **Validated by FEN spot-checks** (mates/wins byte-identical off→on, OCB drawish −0.64→−0.34 toward SF); the `pattern_diag` aggregate is the wrong lens (the scale correctly leaves benign saturated wins at s=1). Constants are SPSA-shapes for later tuning. |
| **Mate-drive material scale** | `advanced_endgame_eval` | parked-knob (`ENABLE_MATE_DRIVE_SCALE`, default-off) | med | Scale the king-drive by winner's material margin (defender-queen→0). Suite-neutral, no definitive self-play; its R+N-vs-R target now handled by is_practically_drawn. Preserved as a knob; redo with a defender-bare gate inside the graded scale above. |
| **`pattern_diag.py` (miseval miner)** | `deep_diag`/`term_diag` helpers | **shipped** (tooling 2026-06-08) | — | Offline: material-config offender clusters / signed-bias-by-material-delta (lex-oriented, fixable-vs-scatter) / transformation-leak scan. Trust-gated (NNUE-static valid only where it tracks SF search). The systematic replacement for eyeballing top-20 offender lists. |
| **Harness concurrency + timed + UHO book** | `tournament.py`/`sprt.py`, `gen_openings.py` | **shipped** (0e02285) | — | `--concurrency N` (measured **6.5×** on 8c/16t; per-thread SF arbiter, main-thread bookkeeping), `--max-minutes` greedy timed mode, UHO unbalanced-book generator (`openings_uho.txt`). The throughput unlock for sound SPRT. |
| **SPSA + SPRT tuning loop** | self-play harness (`tournament.py`, `sprt.py`) | discussed (**throughput prereq SHIPPED**) | high | THE multiplier — SPRT early-stop A/B + SPSA param optimization. Data-hungry; the prerequisite **game concurrency (6.5×) + timed mode + UHO book** now shipped (0e02285), so ~1000-game runs are feasible overnight. |

## How to use
1. Promote `idea`→`discussed`→`designed` as we scope; one `designed` item enters `testing` at a time.
2. A `testing` item gets env-toggled (default-off), runs STS300+WAC, then an overnight adjudicated tournament.
3. `shipped` → move the row's result to `OPTIMIZATION_LOG.md` STRENGTH TRACK; `parked` → note blocker + date here.
